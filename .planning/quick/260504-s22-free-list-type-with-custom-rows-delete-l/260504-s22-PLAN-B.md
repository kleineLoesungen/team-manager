---
phase: 260504-s22
plan: B
type: execute
wave: 1
depends_on: []
files_modified:
  - src/coach/list_settings_handler.php
  - public/index.php
autonomous: true
requirements: []

must_haves:
  truths:
    - "Moderator sees all local columns listed in the list settings page"
    - "Each local column has a delete button in the settings page"
    - "Clicking delete shows a no-JS inline confirmation step before executing"
    - "Confirming delete removes the column and all its cells for that list"
    - "Global columns are not shown in the delete list (cannot delete global columns here)"
  artifacts:
    - path: "src/coach/list_settings_handler.php"
      provides: "Local columns list with delete action; confirmation step; DELETE SQL for column + cells"
    - path: "public/index.php"
      provides: "Route for POST /moderator/lists/{id}/columns/{column_id}/delete"
  key_links:
    - from: "list_settings page"
      to: "columns table"
      via: "DELETE WHERE id=? AND list_id=? AND team_id= (ownership triple-check)"
    - from: "column delete"
      to: "cells table"
      via: "CASCADE via FK (cells.column_id REFERENCES columns ON DELETE CASCADE) — automatic"
---

<objective>
Allow moderators to delete local (list-specific) columns from the list settings page. Uses a two-step inline form confirmation — no JavaScript required.

Purpose: Moderators need to remove outdated or mistaken local columns. Cells cascade-delete via the existing FK.
Output: Delete button per local column on the settings page with no-JS confirm step.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/coach/list_settings_handler.php
@public/index.php
@database/schema.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: List settings — show local columns with delete button</name>
  <files>src/coach/list_settings_handler.php</files>
  <action>
In `src/coach/list_settings_handler.php`, after the existing settings form and before the Gefahrenzone delete-list block, add a new card section that lists local columns and offers deletion.

**Data loading** (at top of handler, after `$list` is fetched):
```php
$local_cols_stmt = $pdo->prepare(
    "SELECT id, name, data_type FROM columns
     WHERE list_id = ? AND team_id = ? AND is_active = TRUE
     ORDER BY sort_order, created_at"
);
$local_cols_stmt->execute([$list_id, $_SESSION['team_id']]);
$local_columns = $local_cols_stmt->fetchAll(PDO::FETCH_ASSOC);
```

**POST handling for column deletion** — add a block at the top of the POST section (before/after the visibility update), guarded by `$_POST['action'] === 'delete_column'`:
```php
if ($_SERVER['REQUEST_METHOD'] === 'POST' && isset($_POST['action']) && $_POST['action'] === 'delete_column') {
    require_csrf();
    $col_id  = (int)($_POST['column_id'] ?? 0);
    $confirm = (int)($_POST['confirm']   ?? 0);

    if ($confirm !== 1) {
        // Show confirmation step — store pending col_id in local var and fall through to render
        $delete_pending_col_id = $col_id;
    } else {
        // Execute deletion — triple ownership check
        try {
            $del = $pdo->prepare(
                "DELETE FROM columns WHERE id = ? AND list_id = ? AND team_id = ?"
            );
            $del->execute([$col_id, $list_id, $_SESSION['team_id']]);
            // Cells cascade-delete automatically via FK (cells.column_id REFERENCES columns ON DELETE CASCADE)
            redirect('/moderator/lists/' . $list_id . '/settings?success=1');
        } catch (PDOException $e) {
            error_log('Column delete error: ' . $e->getMessage());
            $error = 'Fehler beim Löschen der Spalte.';
        }
    }
}
```

**Template section** — inside the `render_coach_page` closure, add a new card after the main settings form:

```php
// Pass $local_columns and $delete_pending_col_id to the closure
render_coach_page('Listen-Einstellungen', 'lists', function() use ($list, $error, $local_columns, $delete_pending_col_id) {
```

In the template HTML:
```html
<?php if (!empty($local_columns)): ?>
<div class="card shadow-sm mt-4">
  <div class="card-body">
    <h6 class="card-title">Lokale Spalten</h6>
    <p class="text-muted small mb-3">Lokale Spalten gehören nur zu dieser Liste. Löschen entfernt auch alle zugehörigen Einträge.</p>
    <ul class="list-group list-group-flush">
      <?php foreach ($local_columns as $col): ?>
      <li class="list-group-item d-flex justify-content-between align-items-center px-0">
        <div>
          <span class="fw-medium"><?= e($col['name']) ?></span>
          <span class="badge bg-light text-dark border ms-2 small">
            <?= match($col['data_type']) { 'boolean' => 'Ja/Nein', 'number' => 'Zahl', 'text' => 'Text', default => e($col['data_type']) } ?>
          </span>
        </div>
        <?php if (isset($delete_pending_col_id) && $delete_pending_col_id === (int)$col['id']): ?>
          <!-- Confirmation step — shown inline after first delete click -->
          <form method="POST" action="/moderator/lists/<?= (int)$list['id'] ?>/settings" class="d-flex gap-2 align-items-center">
            <?= csrf_field() ?>
            <input type="hidden" name="action" value="delete_column">
            <input type="hidden" name="column_id" value="<?= (int)$col['id'] ?>">
            <input type="hidden" name="confirm" value="1">
            <span class="text-danger small me-2">Spalte und alle Einträge löschen?</span>
            <button type="submit" class="btn btn-sm btn-danger min-touch">Ja, löschen</button>
            <a href="/moderator/lists/<?= (int)$list['id'] ?>/settings" class="btn btn-sm btn-outline-secondary min-touch">Abbrechen</a>
          </form>
        <?php else: ?>
          <!-- Initial delete button -->
          <form method="POST" action="/moderator/lists/<?= (int)$list['id'] ?>/settings">
            <?= csrf_field() ?>
            <input type="hidden" name="action" value="delete_column">
            <input type="hidden" name="column_id" value="<?= (int)$col['id'] ?>">
            <input type="hidden" name="confirm" value="0">
            <button type="submit" class="btn btn-sm btn-outline-danger min-touch">Löschen</button>
          </form>
        <?php endif; ?>
      </li>
      <?php endforeach; ?>
    </ul>
  </div>
</div>
<?php endif; ?>
```

Also add `$delete_pending_col_id = null;` near the top of the handler so it is always defined.

No new routes needed — all POST actions go to the existing `/moderator/lists/{id}/settings` route. The handler already handles both GET and POST.
  </action>
  <verify>
1. Open list settings for a list with at least one local column.
2. Verify local column is listed with a "Löschen" button.
3. Click Löschen — same page reloads showing "Spalte und alle Einträge löschen?" with Ja/Abbrechen.
4. Click Abbrechen — column still present.
5. Click Löschen again, then Ja — redirects to settings with column gone. Verify in DB: `SELECT id FROM columns WHERE id={col_id}` returns nothing.
6. Verify cells for that column are also gone: `SELECT id FROM cells WHERE column_id={col_id}` returns nothing (cascade delete).
7. Global columns page (/moderator/columns) unaffected.
  </verify>
  <done>Moderators can delete local columns from list settings. Two-step confirmation works. Cells cascade-delete. Global columns section unaffected.</done>
</task>

</tasks>

<verification>
- List settings page loads without error for all list types
- Local columns section shows only list-specific columns (list_id IS NOT NULL)
- Delete flow: step 1 shows confirm, step 2 executes, success redirect
- cells.column_id FK cascade removes cells automatically
</verification>

<success_criteria>
- Each local column on the settings page has a delete button
- Two-step confirmation (no JS) before deletion
- Column and all associated cells deleted after confirmation
- No regression on existing settings form (visibility, date, description)
</success_criteria>

<output>
After completion, create `.planning/quick/260504-s22-free-list-type-with-custom-rows-delete-l/260504-s22-B-SUMMARY.md`
</output>
