---
phase: quick
plan: 260506-jb7
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migration_008_moderator_to_coordinator.sql
  - deploy.sh
  - README.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "No migration_*.sql files remain in database/"
    - "deploy.sh contains no references to migration files"
    - "README.md accurately describes the current app state and project structure"
  artifacts:
    - path: "database/schema.sql"
      provides: "Complete canonical schema (untouched)"
    - path: "database/rls_policies.sql"
      provides: "Complete canonical RLS policies (untouched)"
    - path: "deploy.sh"
      provides: "Clean deployment script without stale migration references"
    - path: "README.md"
      provides: "Accurate release-ready documentation"
  key_links:
    - from: "README.md"
      to: "database/"
      via: "Datenbankstruktur table"
      pattern: "schema\\.sql|rls_policies\\.sql"
---

<objective>
Remove the last applied migration file from the repository, clean up stale migration
references in deploy.sh, and verify README.md is accurate for a release-ready state.

Purpose: Applied migrations are one-time scripts — keeping them in the repo causes
confusion about what the current schema is. deploy.sh still echoes instructions for
deleted migration files (migrate_004, migrate_005). Clean these up before release.

Output: Clean database/ folder (only schema.sql + rls_policies.sql), clean deploy.sh,
accurate README.md.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete migration_008 and clean deploy.sh migration references</name>
  <files>database/migration_008_moderator_to_coordinator.sql, deploy.sh</files>
  <action>
    1. Delete `database/migration_008_moderator_to_coordinator.sql` — this migration
       (rename moderator→coordinator) was applied to production and is no longer needed.
       Use `git rm database/migration_008_moderator_to_coordinator.sql`.

    2. Edit `deploy.sh` — remove the final 3-line block that references deleted migration
       files. The block to remove is:
       ```
       echo "Upgrading existing DB: run the migration scripts once on the server:"
       echo "  psql -U <user> -d <database> -f database/migrate_004_rename_roles.sql"
       echo "  psql -U <user> -d <database> -f database/migrate_005_rename_member.sql"
       echo "Note: The PHP app also runs these migrations automatically on the first request."
       ```
       These files no longer exist. Replace the entire block (the last 4 echo lines)
       with nothing — the script should end cleanly after the blank `echo ""` line that
       follows "DB tables are created automatically on the first HTTP request."

    Also remove the help-text line in the usage section that says:
    `echo "  src/ database/                        → alongside index.php"`
    — wait, this is accurate (database/ is still deployed), leave it.

    Only remove the final migration-instructions block at the bottom of the script.
  </action>
  <verify>
    <automated>
      ls /Users/sebastianwiller/Documents/github/team-manager/database/ | grep -v migration | grep -c "\.sql" && \
      ! ls /Users/sebastianwiller/Documents/github/team-manager/database/ | grep -q migration && \
      ! grep -q "migrate_" /Users/sebastianwiller/Documents/github/team-manager/deploy.sh && \
      echo "PASS: migration file deleted, deploy.sh clean"
    </automated>
  </verify>
  <done>
    `database/` contains only schema.sql and rls_policies.sql.
    `deploy.sh` contains no references to migrate_* files.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify and update README.md for release accuracy</name>
  <files>README.md</files>
  <action>
    Review README.md for accuracy against current codebase state. Make targeted corrections only:

    1. **Projektstruktur section** — the src/ subdirectory listing shows `admin/         Admin-Handler (Teams, Trainer)`.
       Trainers were renamed to Koordinatoren in the UI, and the DB role is `coordinator`.
       Update the comment to: `admin/         Admin-Handler (Teams, Koordinatoren)`

    2. **Projektstruktur section** — `templates/` subdirectory listing: currently lists sub-paths
       implicitly. No change needed if the directory structure is accurate.

    3. **No other README changes needed** — the Docker section, deployment section, and
       Datenbankstruktur table already correctly list only schema.sql and rls_policies.sql
       (no migration files are referenced in the init table).

    4. **Do NOT add a migration history section** — migrations are deleted, history lives in git.

    If on reading the README you find any other references to "Trainer", "Coach", "Spieler",
    or "Moderator" as role names (outside of historical context), update them to the current
    German terms: "Koordinator" (coordinator role) and "Mitglied" (member role).
    The login table shows "Admin" only — that is correct.
  </action>
  <verify>
    <automated>
      ! grep -i "migrate_" /Users/sebastianwiller/Documents/github/team-manager/README.md && \
      echo "PASS: README has no stale migration references"
    </automated>
  </verify>
  <done>
    README.md is accurate: role names match codebase, no stale migration references,
    database init table lists only the two canonical SQL files.
  </done>
</task>

</tasks>

<verification>
After both tasks:
- `ls database/` shows exactly: `rls_policies.sql  schema.sql`
- `grep -r "migrate_" database/ deploy.sh README.md` returns nothing
- `git status` shows deletions and edits staged/committed cleanly
</verification>

<success_criteria>
- Zero migration_*.sql files in `database/`
- `deploy.sh` ends cleanly after the first-deploy instruction, no stale migration echo lines
- `README.md` role names (Koordinator/Mitglied) match current codebase usage
- `git log` shows a clean release-prep commit
</success_criteria>

<output>
After completion, create `.planning/quick/260506-jb7-remove-all-migrations-from-source-code-a/260506-jb7-SUMMARY.md`
</output>
