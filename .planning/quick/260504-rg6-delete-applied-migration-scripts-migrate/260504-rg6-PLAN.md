---
phase: quick
plan: 260504-rg6
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrate_004_rename_roles.sql
  - database/migrate_005_rename_member.sql
autonomous: true
requirements: []
must_haves:
  truths:
    - "migrate_004_rename_roles.sql no longer exists in the repository"
    - "migrate_005_rename_member.sql no longer exists in the repository"
  artifacts: []
  key_links: []
---

<objective>
Delete the two one-time migration scripts that have already been applied to production.

Purpose: Applied migrations left in the repository are misleading — they imply the scripts can or should be re-run. Removing them keeps the database/ directory clean and reflects the current schema state accurately.
Output: Two files deleted from the repository.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete applied migration scripts</name>
  <files>database/migrate_004_rename_roles.sql, database/migrate_005_rename_member.sql</files>
  <action>
    Delete both files using git rm so the removal is staged for commit:

    git rm database/migrate_004_rename_roles.sql database/migrate_005_rename_member.sql

    These scripts performed one-time data migrations (renaming role values) and have been successfully applied to production. They must not be re-run and serve no further purpose in the repository.
  </action>
  <verify>
    <automated>test ! -f /Users/sebastianwiller/Documents/github/team-manager/database/migrate_004_rename_roles.sql && test ! -f /Users/sebastianwiller/Documents/github/team-manager/database/migrate_005_rename_member.sql && echo "DELETED OK"</automated>
  </verify>
  <done>Both files are absent from the working tree and staged for removal in git.</done>
</task>

</tasks>

<verification>
Run: ls /Users/sebastianwiller/Documents/github/team-manager/database/
Neither migrate_004 nor migrate_005 file should appear in the listing.
</verification>

<success_criteria>
Both migration files are removed from the repository. No other files are modified.
</success_criteria>

<output>
After completion, create `.planning/quick/260504-rg6-delete-applied-migration-scripts-migrate/260504-rg6-SUMMARY.md`
</output>
