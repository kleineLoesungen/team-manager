---
phase: quick-260517-d33
plan: 01
subsystem: marketing
tags: [landing-page, product, html, deploy]
dependency_graph:
  requires: []
  provides: [product.html, deploy-exclusion]
  affects: [deploy.sh]
tech_stack:
  added: [Bootstrap Icons 1.11.3, Inter font via Google Fonts]
  patterns: [single-file HTML, CDN-only, CSS custom properties]
key_files:
  created:
    - product.html
  modified:
    - deploy.sh
decisions:
  - product.html stays at project root and is excluded from FTP deploy via --exclude flag
  - CTA buttons use placeholder href="#" since no live app URL is defined
  - Bootstrap 5.3 SRI hashes used matching the rest of the project
metrics:
  duration: "~8 minutes"
  completed: "2026-05-17"
  tasks: 2
  files: 2
---

# Phase quick-260517-d33 Plan 01: German Product Landing Page Summary

**One-liner:** Single-file German landing page (product.html) with hero/features/CTA sections using Bootstrap 5.3, excluded from FTP deploy via deploy.sh.

## What Was Built

- `product.html` at the project root: a standalone, self-contained German product/marketing page for the Team Manager app. No build step required — works on `file://` protocol directly.
- `deploy.sh` updated: `--exclude='product.html'` added to the first `mirror --reverse` command, preventing the page from being uploaded to the Hetzner FTP server.

## Task Results

| Task | Description | Commit | Status |
|------|-------------|--------|--------|
| 1 | Create product.html — German landing page | 0a7e07e | Done |
| 2 | Exclude product.html from deploy.sh | 7c3301f | Done |

## Page Sections (in order)

1. **Navbar** — Sticky, dark navy background, "TeamManager" logo with accent-colored "Manager"
2. **Hero** — Full-viewport, dark navy gradient with indigo radial overlay and decorative SVG blob. Headline: "Dein Team. Deine Listen. Deine Statistiken." Primary CTA: "Jetzt kostenlos starten"
3. **Features** — 3-column card grid (single column on mobile): Flexible Listen, Mitglieder tragen selbst ein, Statistik auf einen Blick. Bootstrap Icons for card icons.
4. **Quote** — Single centered pull-quote: "Endlich weiß ich auf einen Blick, wer wie oft mitgemacht hat." — Eine Koordinatorin
5. **CTA Section** — Indigo gradient background. Headline: "Bereit loszulegen?" White button.
6. **Footer** — Dark background, centered copyright line.

## Deviations from Plan

None — plan executed exactly as written.

## Known Stubs

- CTA button `href="#"` is intentional — no live app URL is defined. Not a blocker for the page's goal (presentation/marketing).

## Self-Check: PASSED

- `product.html` exists at project root
- `deploy.sh` contains `--exclude='product.html'`
- Commits `0a7e07e` and `7c3301f` exist in git log
