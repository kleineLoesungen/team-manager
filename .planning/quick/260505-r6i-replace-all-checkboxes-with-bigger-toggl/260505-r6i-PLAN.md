---
phase: quick
plan: 260505-r6i
type: execute
wave: 1
depends_on: []
files_modified:
  - src/coordinator/list_settings_handler.php
  - src/templates/coordinator/stats.php
  - src/templates/coordinator/list_form.php
  - src/templates/coordinator/list_row_form.php
  - src/templates/member/list_row_form.php
autonomous: true
requirements: []

must_haves:
  truths:
    - "Every checkbox in the app renders as a form-switch toggle"
    - "All toggles use width:3em;height:1.75em;cursor:pointer; on the input"
    - "Toggle labels are vertically centered using d-flex align-items-center gap-2 on the wrapper"
    - "No plain <div class=\"form-check\"> without form-switch remains for checkbox inputs"
  artifacts:
    - path: "src/coordinator/list_settings_handler.php"
      provides: "show_all_rows and is_hidden as form-switch toggles"
    - path: "src/templates/coordinator/stats.php"
      provides: "include_undated as form-switch toggle"
    - path: "src/templates/coordinator/list_form.php"
      provides: "global_columns[] and defaults[col_id] boolean as form-switch toggles; show_all_rows already form-switch verified with correct size"
    - path: "src/templates/coordinator/list_row_form.php"
      provides: "boolean cell toggle at consistent 3em/1.75em size"
    - path: "src/templates/member/list_row_form.php"
      provides: "boolean cell toggle at consistent 3em/1.75em size"
  key_links:
    - from: "list_settings_handler.php"
      to: "form POST"
      via: "show_all_rows and is_hidden checkbox names unchanged"
    - from: "list_form.php"
      to: "form POST"
      via: "global_columns[] and defaults[col_id] names unchanged"
---

<objective>
Replace all remaining plain Bootstrap checkboxes with consistently-sized form-switch toggles, and normalise sizes on existing toggles that are oversized.

Purpose: Visual consistency — every boolean input across the app uses the same toggle style (3em × 1.75em), with vertically-centred labels.
Output: 5 modified PHP files; list_detail.php already compliant and left untouched.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md

Target pattern for every toggle wrapper:
```html
<div class="form-check form-switch d-flex align-items-center gap-2">
    <input class="form-check-input" type="checkbox" role="switch"
           style="width:3em;height:1.75em;cursor:pointer;"
           name="..." id="..." value="1" [checked]>
    <label class="form-check-label mb-0" for="...">Label text</label>
</div>
```

Existing compliant files (do NOT touch):
- src/templates/coordinator/list_detail.php — coach_only toggles and inline boolean cells already use width:3em;height:1.75em;cursor:pointer;
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert plain checkboxes in list_settings_handler.php and stats.php</name>
  <files>src/coordinator/list_settings_handler.php, src/templates/coordinator/stats.php</files>
  <action>
In list_settings_handler.php (lines ~173-191):

Replace the `show_all_rows` block — currently:
```html
<div class="form-check">
    <input class="form-check-input" type="checkbox" name="show_all_rows" id="show_all_rows" value="1" ...>
    <label class="form-check-label" for="show_all_rows">Mitglieder sehen Einträge anderer Mitglieder</label>
</div>
```
With:
```html
<div class="form-check form-switch d-flex align-items-center gap-2">
    <input class="form-check-input" type="checkbox" role="switch"
           style="width:3em;height:1.75em;cursor:pointer;"
           name="show_all_rows" id="show_all_rows" value="1"
           <?= $list['show_all_rows'] ? 'checked' : '' ?>>
    <label class="form-check-label mb-0" for="show_all_rows">
        Mitglieder sehen Einträge anderer Mitglieder
    </label>
</div>
```

Replace the `is_hidden` block — currently plain form-check — with the same pattern:
```html
<div class="form-check form-switch d-flex align-items-center gap-2">
    <input class="form-check-input" type="checkbox" role="switch"
           style="width:3em;height:1.75em;cursor:pointer;"
           name="is_hidden" id="is_hidden" value="1"
           <?= $list['is_hidden'] ? 'checked' : '' ?>>
    <label class="form-check-label mb-0" for="is_hidden">
        Liste verstecken (erscheint eingeklappt am Ende der Übersicht)
    </label>
</div>
```

In stats.php (lines ~31-37):

The `include_undated` checkbox sits inside a `col-auto d-flex align-items-end pb-1` column. Replace:
```html
<div class="form-check mb-0">
    <input class="form-check-input" type="checkbox" name="include_undated" id="include_undated" value="1" ...>
    <label class="form-check-label small" for="include_undated">Ohne Datum einschließen</label>
</div>
```
With:
```html
<div class="form-check form-switch d-flex align-items-center gap-2 mb-0">
    <input class="form-check-input" type="checkbox" role="switch"
           style="width:3em;height:1.75em;cursor:pointer;"
           name="include_undated" id="include_undated" value="1"
           <?= $filter_include_undated ? 'checked' : '' ?>>
    <label class="form-check-label mb-0" for="include_undated">Ohne Datum einschließen</label>
</div>
```
  </action>
  <verify>grep -n "form-check form-switch d-flex" /Users/sebastianwiller/Documents/github/team-manager/src/coordinator/list_settings_handler.php && grep -n "form-check form-switch d-flex" /Users/sebastianwiller/Documents/github/team-manager/src/templates/coordinator/stats.php</verify>
  <done>Both files show at least one form-switch d-flex match; no plain "form-check\">" without form-switch remains for these inputs</done>
</task>

<task type="auto">
  <name>Task 2: Convert global_columns and defaults checkboxes in list_form.php; normalise show_all_rows size</name>
  <files>src/templates/coordinator/list_form.php</files>
  <action>
In list_form.php around line 70, the existing show_all_rows form-switch uses `style="min-height:1.75em;"` on the wrapper but lacks `d-flex align-items-center gap-2`. Update that wrapper div to:
```html
<div class="form-check form-switch d-flex align-items-center gap-2">
```
Remove the `style="min-height:1.75em;"` from the wrapper (the input already has the explicit height). The input already has correct style — leave it. Add `mb-0` to the label.

Around lines 94-104, convert `global_columns[]` plain form-check to form-switch:
```html
<div class="form-check form-switch d-flex align-items-center gap-2">
    <input class="form-check-input" type="checkbox" role="switch"
           style="width:3em;height:1.75em;cursor:pointer;"
           name="global_columns[]" value="<?= $col_id ?>"
           id="col_<?= $col_id ?>" checked>
    <label class="form-check-label mb-0" for="col_<?= $col_id ?>">
        <?= e($col['name']) ?>
        <span class="badge bg-light text-dark border ms-1">
            <?= $col['data_type'] === 'boolean' ? 'Ja/Nein' : 'Zahl' ?>
        </span>
    </label>
</div>
```

Around lines 108-115, convert `defaults[col_id]` boolean form-check-sm to form-switch:
```html
<div class="form-check form-switch d-flex align-items-center gap-2">
    <input class="form-check-input" type="checkbox" role="switch"
           style="width:3em;height:1.75em;cursor:pointer;"
           name="defaults[<?= $col_id ?>]" value="1"
           id="default_<?= $col_id ?>">
    <label class="form-check-label mb-0 text-muted small" for="default_<?= $col_id ?>">
        Standardwert: Ja
    </label>
</div>
```
  </action>
  <verify>grep -n "form-check form-switch d-flex" /Users/sebastianwiller/Documents/github/team-manager/src/templates/coordinator/list_form.php</verify>
  <done>At least 3 matches in list_form.php (show_all_rows, global_columns, defaults); no plain form-check without form-switch for checkbox inputs</done>
</task>

<task type="auto">
  <name>Task 3: Normalise boolean cell toggle size in coordinator and member list_row_form.php</name>
  <files>src/templates/coordinator/list_row_form.php, src/templates/member/list_row_form.php</files>
  <action>
Both files currently use `style="width:3.5em;height:2em;"` (oversized vs target 3em/1.75em) and the wrapper uses `style="min-height:2em;"` instead of `d-flex align-items-center gap-2`.

In src/templates/coordinator/list_row_form.php (around line 44):
Change the wrapper from:
```html
<div class="form-check form-switch" style="min-height:2em;">
```
to:
```html
<div class="form-check form-switch d-flex align-items-center gap-2">
```
Change the input style from `width:3.5em;height:2em;` to `width:3em;height:1.75em;cursor:pointer;` (cursor:pointer already present — just update the dimensions).
Add `mb-0` to the label.

Apply the identical change in src/templates/member/list_row_form.php (around line 39).
  </action>
  <verify>grep -n "3em;height:1.75em" /Users/sebastianwiller/Documents/github/team-manager/src/templates/coordinator/list_row_form.php && grep -n "3em;height:1.75em" /Users/sebastianwiller/Documents/github/team-manager/src/templates/member/list_row_form.php</verify>
  <done>Both files show width:3em;height:1.75em in the boolean cell toggle; no 3.5em or 2em sizes remain</done>
</task>

</tasks>

<verification>
After all three tasks:

```bash
# No plain form-check (without form-switch) wrapping a checkbox should remain in the 5 touched files
grep -rn 'class="form-check"' \
  src/coordinator/list_settings_handler.php \
  src/templates/coordinator/stats.php \
  src/templates/coordinator/list_form.php \
  src/templates/coordinator/list_row_form.php \
  src/templates/member/list_row_form.php

# All toggles use the target size
grep -rn "3em;height:1.75em" \
  src/coordinator/list_settings_handler.php \
  src/templates/coordinator/stats.php \
  src/templates/coordinator/list_form.php \
  src/templates/coordinator/list_row_form.php \
  src/templates/member/list_row_form.php

# No oversized (3.5em / 2em) toggles remain in row forms
grep -rn "3\.5em\|height:2em" \
  src/templates/coordinator/list_row_form.php \
  src/templates/member/list_row_form.php
```

The first grep must produce no output. The second must produce at least one match per file. The third must produce no output.
</verification>

<success_criteria>
- Every boolean checkbox in the app uses Bootstrap form-switch with role="switch"
- All toggle inputs use style="width:3em;height:1.75em;cursor:pointer;"
- All toggle wrappers use class="form-check form-switch d-flex align-items-center gap-2"
- Labels use mb-0 for vertical alignment with the centred wrapper
- No plain <div class="form-check"> without form-switch wraps a checkbox input
- list_detail.php untouched (already compliant)
- Radio buttons in list_form.php (list_type) untouched
</success_criteria>

<output>
After completion, create `.planning/quick/260505-r6i-replace-all-checkboxes-with-bigger-toggl/260505-r6i-SUMMARY.md`
</output>
