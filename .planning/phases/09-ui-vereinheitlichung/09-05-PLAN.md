---
phase: 09-ui-vereinheitlichung
plan: 05
type: execute
wave: 3
depends_on: [09-02]
files_modified:
  - src/templates/coordinator/ticker.php
  - src/templates/coordinator/ticker_detail.php
  - src/templates/coordinator/ticker_form.php
  - src/templates/coordinator/ticker_delete_confirm.php
  - src/templates/coordinator/file_detail.php
  - src/templates/coordinator/file_form.php
  - src/templates/coordinator/file_notify.php
  - src/templates/coordinator/stats.php
  - src/templates/coordinator/coordinators.php
autonomous: true
requirements: []

must_haves:
  truths:
    - "All 9 coordinator ticker/file/stats templates have zero inline style= violations"
    - "All 9 templates have zero py-5, mb-5 spacing violations"
    - "ticker_delete_confirm.php uses the confirmation page pattern (btn-danger, no JS)"
    - "ticker.php uses render_empty() for empty ticker list state"
    - "stats.php uses render_filter_pills() for the stat filter bar"
  artifacts:
    - path: "src/templates/coordinator/ticker.php"
      provides: "Ticker overview with render_empty, render_collection_group partials"
    - path: "src/templates/coordinator/stats.php"
      provides: "Stats page with render_filter_pills for filter bar"
    - path: "src/templates/coordinator/ticker_delete_confirm.php"
      provides: "Confirmation page with btn-danger and cancel link"
  key_links:
    - from: "src/templates/coordinator/stats.php"
      to: "render_filter_pills()"
      via: "stat filter bar (list selector or date range)"
      pattern: "render_filter_pills"
    - from: "src/templates/coordinator/ticker_delete_confirm.php"
      to: "UI-SPEC confirmation pattern"
      via: "Bestätigungsseite archetype: btn-danger + Abbrechen link, no JS confirm"
      pattern: "btn-danger"
---

<objective>
Migrate coordinator ticker, file, stats, and other remaining templates.

Purpose: 9 templates covering the live ticker workflow, markdown file management, email
notifications, statistics view, and coordinator listing. Ticker templates include the
confirmation page pattern (Bestätigungsseite archetype).

Output: 9 coordinator templates migrated, zero violations, confirmation page pattern applied.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-ui-vereinheitlichung/09-UI-SPEC.md
@.planning/phases/09-ui-vereinheitlichung/09-RESEARCH.md
@.planning/UI-BASELINE.md
@.planning/phases/09-ui-vereinheitlichung/09-02-SUMMARY.md
</context>

<interfaces>
<!-- Partials available everywhere — no require_once needed: -->
render_flash(string $type, string $message): void
render_empty(string $icon, string $heading, string $body_text, ?string $action_html): void
render_badge(string $type, string $label): void
render_collection_group(string $label, callable $items_body): void
render_filter_pills(array $pills, string $base_url): void
render_danger_zone(string $action_label, string $description, string $form_html): void
render_form_section(string $heading, callable $fields_body, ?string $footer_html): void
render_form_field(string $label, string $input_html, ?string $hint): void
render_matrix_table(array $columns, callable $rows_body, ?callable $footer_body): void
render_action_bar(string $label, string $form_id): void

<!-- Confirmation page archetype (UI-SPEC): -->
// Heading: "Möchtest du [X] wirklich löschen?"
// Body: "Diese Aktion kann nicht rückgängig gemacht werden."
// Confirm button: btn btn-danger
// Cancel: btn btn-outline-secondary OR text link "Abbrechen"
// NO JavaScript confirm dialog
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Migrate ticker.php, ticker_detail.php, ticker_form.php, ticker_delete_confirm.php</name>
  <files>
    src/templates/coordinator/ticker.php,
    src/templates/coordinator/ticker_detail.php,
    src/templates/coordinator/ticker_form.php,
    src/templates/coordinator/ticker_delete_confirm.php
  </files>

  <read_first>
  - src/templates/coordinator/ticker.php — read ENTIRE file
  - src/templates/coordinator/ticker_detail.php — read ENTIRE file
  - src/templates/coordinator/ticker_form.php — read ENTIRE file
  - src/templates/coordinator/ticker_delete_confirm.php — read ENTIRE file
  - src/templates/components/partials.php — review signatures
  </read_first>

  <action>
  Apply the standard violation checklist to all 4 files:
  - Remove all `style="..."` attributes (except img onerror)
  - Replace py-5 empty states → render_empty()
  - Replace mb-5 → mb-4
  - Remove shadow-sm from cards
  - Remove max-width inline styles from cards
  - Replace form-control-sm on form inputs → remove -sm
  - Replace badge inline patterns → render_badge()
  - Add ?success=1 flash check at top of body where appropriate
  - Remove style="font-size:2rem;" from icons

  **ticker.php specific changes:**
  - Overview of all tickers for this team. If empty:
    ```php
    <?php render_empty('megaphone', 'Noch keine Ticker', 'Erstelle den ersten Ticker für ein Event.'); ?>
    ```
  - Ticker status badges: per UI-SPEC color rules —
    - Active/running ticker: render_badge('ok', 'Aktiv') or render_badge('ok', 'Läuft')
    - Closed ticker: render_badge('dim', 'Beendet')
  - If tickers grouped by status, use render_collection_group().

  **ticker_detail.php specific changes:**
  - Live ticker detail view with message list and post form.
  - Message posting form: ensure form-control without -sm on the message textarea.
  - Tag selector (if present): form-select without -sm.
  - Switch inputs (member write access toggle if shown): remove inline style.
  - Status badge for the ticker itself: render_badge('ok'/'dim', ...).

  **ticker_form.php specific changes:**
  - Form for creating/editing a ticker event.
  - Ensure all inputs (event name, date, description) use form-control/form-select without -sm.
  - If using a card wrapper, remove shadow-sm.
  - Can use render_form_section() and render_form_field() for form structure.

  **ticker_delete_confirm.php specific changes (IMPORTANT — Bestätigungsseite archetype):**
  - This is a two-step confirmation page (no JavaScript). It must follow the Bestätigungsseite pattern:
  - Page heading: `<h1 class="h2 mb-3">Möchtest du diesen Ticker wirklich löschen?</h1>`
  - Body text: `<p class="text-body-secondary">Diese Aktion kann nicht rückgängig gemacht werden.</p>`
  - Confirm button: `<button type="submit" class="btn btn-danger w-100 mb-2">Ja, löschen</button>`
  - Cancel: `<a href="/coordinator/ticker" class="btn btn-outline-secondary w-100">Abbrechen</a>`
  - NO JavaScript confirm() call.
  - Remove any inline styles that may be present.
  - The page itself should NOT have a danger zone card — it IS the confirmation page.
  </action>

  <verify>
    <automated>
    php -l /Users/sebastianwiller/Documents/github/team-manager/src/templates/coordinator/ticker.php && \
    php -l /Users/sebastianwiller/Documents/github/team-manager/src/templates/coordinator/ticker_detail.php && \
    php -l /Users/sebastianwiller/Documents/github/team-manager/src/templates/coordinator/ticker_form.php && \
    php -l /Users/sebastianwiller/Documents/github/team-manager/src/templates/coordinator/ticker_delete_confirm.php
    # All must exit 0

    grep -n 'style="' /Users/sebastianwiller/Documents/github/team-manager/src/templates/coordinator/ticker.php | grep -v "onerror"
    grep -n 'style="' /Users/sebastianwiller/Documents/github/team-manager/src/templates/coordinator/ticker_detail.php | grep -v "onerror"
    grep -n 'style="' /Users/sebastianwiller/Documents/github/team-manager/src/templates/coordinator/ticker_delete_confirm.php | grep -v "onerror"
    # All must produce NO output

    grep -c "btn-danger" /Users/sebastianwiller/Documents/github/team-manager/src/templates/coordinator/ticker_delete_confirm.php
    # Must output: >= 1

    grep -c "render_empty" /Users/sebastianwiller/Documents/github/team-manager/src/templates/coordinator/ticker.php
    # Must output: >= 1
    </automated>
  </verify>

  <acceptance_criteria>
  - All 4 files pass `php -l`
  - Zero `style="` violations (excluding onerror) in all 4 files
  - Zero `py-5`, `mb-5`, `shadow-sm` in all 4 files
  - ticker.php contains `render_empty(` or `render_collection_group(`
  - ticker_delete_confirm.php contains `btn-danger` (confirm button)
  - ticker_delete_confirm.php contains `Abbrechen` cancel link
  - ticker_delete_confirm.php has NO JavaScript confirm() call
  </acceptance_criteria>

  <done>4 ticker templates migrated. Confirmation page follows Bestätigungsseite archetype. Ticker list uses partials.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate file_detail.php, file_form.php, file_notify.php, stats.php, coordinators.php</name>
  <files>
    src/templates/coordinator/file_detail.php,
    src/templates/coordinator/file_form.php,
    src/templates/coordinator/file_notify.php,
    src/templates/coordinator/stats.php,
    src/templates/coordinator/coordinators.php
  </files>

  <read_first>
  - src/templates/coordinator/file_detail.php — read ENTIRE file
  - src/templates/coordinator/file_form.php — read ENTIRE file
  - src/templates/coordinator/file_notify.php — read ENTIRE file
  - src/templates/coordinator/stats.php — read ENTIRE file (contains filter bar and stats table)
  - src/templates/coordinator/coordinators.php — read ENTIRE file
  - src/templates/components/partials.php — review render_filter_pills, render_matrix_table
  </read_first>

  <action>
  Apply the standard violation checklist to all 5 files:
  - Remove all `style="..."` attributes (except img onerror)
  - Replace py-5 → render_empty(), mb-5 → mb-4
  - Remove shadow-sm from cards, max-width inline styles
  - Replace form-control-sm → form-control, form-select-sm → form-select
  - Replace badge inline patterns → render_badge()
  - Add ?success=1 flash where applicable

  **file_detail.php specific changes:**
  - Markdown file detail view. May have a rendered Markdown preview area.
  - If there is a danger zone for file deletion, replace with render_danger_zone().
  - Notify button area: no inline styles.

  **file_form.php specific changes:**
  - Form for creating/editing a markdown file.
  - Textarea for markdown content: `<textarea class="form-control" ...>` without -sm.
  - Title input: form-control without -sm.
  - Visibility selector: form-select without -sm.

  **file_notify.php specific changes:**
  - Email notification review/send form for a file. Similar to list_notify.php.
  - Subject/message inputs: form-control without -sm.
  - Recipient info display: no inline styles.

  **stats.php specific changes (IMPORTANT — filter bar and matrix table):**
  - The coordinator stats page has a filter bar (list selector, date range) and a stats table.
  - Filter bar: if filters are rendered as GET-linked pills or a select dropdown:
    - Pills: use render_filter_pills(). Build $pills array with 'label', 'url', 'active' keys.
    - If it's a form with select: wrap in a form, use form-select without -sm.
  - Stats table: the aggregation table (members × columns) should use render_matrix_table().
    ```php
    <?php render_matrix_table($column_headers, function() use ($stats_rows) {
        foreach ($stats_rows as $row) { ?>
            <tr>
                <td><?= e($row['name']) ?></td>
                <?php foreach ($row['values'] as $v): ?>
                <td><?= e($v) ?></td>
                <?php endforeach; ?>
            </tr>
        <?php }
    }); ?>
    ```
  - Leaderboard ranking section: no inline styles. Remove shadow-sm from any ranking cards.
  - Remove form-control-sm from any filter inputs.

  **coordinators.php specific changes:**
  - Coordinator profile page showing the team's coordinator(s).
  - Standard checklist. If there's a list of coordinators, apply grouping partials if grouped.
  - Status badges → render_badge().
  </action>

  <verify>
    <automated>
    php -l /Users/sebastianwiller/Documents/github/team-manager/src/templates/coordinator/file_detail.php && \
    php -l /Users/sebastianwiller/Documents/github/team-manager/src/templates/coordinator/file_form.php && \
    php -l /Users/sebastianwiller/Documents/github/team-manager/src/templates/coordinator/file_notify.php && \
    php -l /Users/sebastianwiller/Documents/github/team-manager/src/templates/coordinator/stats.php && \
    php -l /Users/sebastianwiller/Documents/github/team-manager/src/templates/coordinator/coordinators.php
    # All must exit 0

    grep -n 'style="' /Users/sebastianwiller/Documents/github/team-manager/src/templates/coordinator/stats.php | grep -v "onerror"
    grep -n 'style="' /Users/sebastianwiller/Documents/github/team-manager/src/templates/coordinator/file_detail.php | grep -v "onerror"
    # Both must produce NO output

    grep -c "render_matrix_table\|render_filter_pills" /Users/sebastianwiller/Documents/github/team-manager/src/templates/coordinator/stats.php
    # Must output: >= 1
    </automated>
  </verify>

  <acceptance_criteria>
  - All 5 files pass `php -l`
  - Zero `style="` violations (excluding onerror) in all 5 files
  - Zero `py-5`, `mb-5`, `shadow-sm` in all 5 files
  - stats.php contains `render_matrix_table(` or `render_filter_pills(`
  - file_detail.php contains `render_danger_zone(` if a delete danger zone exists
  - file_form.php has `<textarea class="form-control"` (no -sm)
  - coordinators.php has no inline styles
  </acceptance_criteria>

  <done>5 coordinator file/stats templates migrated. Stats page uses partials for filter bar and matrix table. All violations removed.</done>
</task>

</tasks>

<verification>
Run across all 9 files:
```bash
grep -rn 'style="' \
  /Users/sebastianwiller/Documents/github/team-manager/src/templates/coordinator/ticker.php \
  /Users/sebastianwiller/Documents/github/team-manager/src/templates/coordinator/ticker_detail.php \
  /Users/sebastianwiller/Documents/github/team-manager/src/templates/coordinator/ticker_form.php \
  /Users/sebastianwiller/Documents/github/team-manager/src/templates/coordinator/ticker_delete_confirm.php \
  /Users/sebastianwiller/Documents/github/team-manager/src/templates/coordinator/file_detail.php \
  /Users/sebastianwiller/Documents/github/team-manager/src/templates/coordinator/file_form.php \
  /Users/sebastianwiller/Documents/github/team-manager/src/templates/coordinator/file_notify.php \
  /Users/sebastianwiller/Documents/github/team-manager/src/templates/coordinator/stats.php \
  /Users/sebastianwiller/Documents/github/team-manager/src/templates/coordinator/coordinators.php | grep -v "onerror"
```
Should produce NO output.
</verification>

<success_criteria>
- All 9 coordinator ticker/file/stats templates pass php -l
- Zero inline style violations across all 9 files
- Confirmation page pattern applied to ticker_delete_confirm.php
- Stats page uses render_matrix_table or render_filter_pills
</success_criteria>

<output>
After completion, create `.planning/phases/09-ui-vereinheitlichung/09-05-SUMMARY.md`
</output>
