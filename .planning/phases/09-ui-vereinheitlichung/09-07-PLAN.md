---
phase: 09-ui-vereinheitlichung
plan: 07
type: execute
wave: 3
depends_on: [09-02]
files_modified:
  - src/templates/admin/dashboard.php
  - src/templates/admin/team_create.php
  - src/templates/admin/team_edit.php
  - src/templates/admin/team_form.php
  - src/templates/admin/coach_form.php
  - src/templates/admin/coordinator_settings.php
  - src/templates/admin/coordinator_edit_email.php
  - src/templates/admin/columns.php
  - src/templates/admin/column_delete_confirm.php
  - src/templates/admin/column_merge_confirm.php
  - src/templates/admin/column_rename.php
autonomous: true
requirements: []

must_haves:
  truths:
    - "All 11 admin templates in this plan have zero inline style= violations"
    - "All 11 admin templates have zero py-5, mb-5 violations"
    - "credential_modal.php is NOT modified (excluded per Pitfall 3)"
    - "Confirmation pages (column_delete_confirm, column_merge_confirm) follow Bestätigungsseite pattern"
    - "Admin team and coordinator list views use render_empty and render_collection_group"
    - "All form pages have form-control without -sm suffix"
  artifacts:
    - path: "src/templates/admin/dashboard.php"
      provides: "Admin team overview with render_empty, render_collection_group"
    - path: "src/templates/admin/column_delete_confirm.php"
      provides: "Bestätigungsseite with btn-danger + cancel link"
  key_links:
    - from: "src/templates/admin/dashboard.php"
      to: "render_collection_group()"
      via: "Active/inactive team grouping"
      pattern: "render_collection_group"
    - from: "src/templates/admin/column_delete_confirm.php"
      to: "UI-SPEC Bestätigungsseite archetype"
      via: "btn-danger confirm, cancel link"
      pattern: "btn-danger"
---

<objective>
Migrate the first 11 admin role templates (team/coordinator/column management). credential_modal.php is explicitly excluded.

Purpose: The admin role is split across two plans (09-07 and 09-07b) for context budget. This plan covers team management (dashboard, team_create, team_edit, team_form, coach_form) and coordinator/column management (coordinator_settings, coordinator_edit_email, columns, column_delete_confirm, column_merge_confirm, column_rename).

Output: 11 admin templates migrated, zero violations, confirmation pages follow Bestätigungsseite pattern.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-ui-vereinheitlichung/09-UI-SPEC.md
@.planning/phases/09-ui-vereinheitlichung/09-RESEARCH.md
@.planning/UI-BASELINE.md
@.planning/phases/09-ui-vereinheitlichung/09-02-SUMMARY.md
</context>

<interfaces>
<!-- CRITICAL EXCLUSION: Do NOT touch src/templates/admin/credential_modal.php -->
<!-- Reason (Pitfall 3): credential_modal sends Cache-Control: no-store header before HTML. -->
<!-- It is rendered by handlers via include, not via a layout function. Do not migrate. -->

<!-- Partials available everywhere — no require_once needed: -->
render_flash(string $type, string $message): void
render_empty(string $icon, string $heading, string $body_text, ?string $action_html): void
render_badge(string $type, string $label): void
render_collection_group(string $label, callable $items_body): void
render_form_section(string $heading, callable $fields_body, ?string $footer_html): void
render_form_field(string $label, string $input_html, ?string $hint): void
render_danger_zone(string $action_label, string $description, string $form_html): void
render_action_bar(string $label, string $form_id): void
render_page_header(string $title, ?string $back_url, ?string $action_html): void
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Migrate dashboard.php, team_create.php, team_edit.php, team_form.php, coach_form.php</name>
  <files>
    src/templates/admin/dashboard.php,
    src/templates/admin/team_create.php,
    src/templates/admin/team_edit.php,
    src/templates/admin/team_form.php,
    src/templates/admin/coach_form.php
  </files>

  <read_first>
  - src/templates/admin/dashboard.php — read ENTIRE file
  - src/templates/admin/team_create.php — read ENTIRE file
  - src/templates/admin/team_edit.php — read ENTIRE file
  - src/templates/admin/team_form.php — read ENTIRE file
  - src/templates/admin/coach_form.php — read ENTIRE file
  - src/templates/components/partials.php — review signatures
  </read_first>

  <action>
  Apply the standard violation checklist to all 5 files:
  - Remove all `style="..."` attributes (except img onerror)
  - Replace py-5 empty states → render_empty()
  - Replace mb-5 → mb-4; remove *-0, *-1 spacing utilities
  - Remove shadow-sm from cards, max-width inline styles
  - Replace form-control-sm, form-select-sm → remove -sm suffix
  - Replace badge inline patterns → render_badge()
  - Add ?success=1 flash check at top of body where applicable

  **dashboard.php specific changes:**
  - Admin dashboard showing teams (active/inactive grouping exists from a quick task).
  - Active teams group: render_collection_group('Aktive Teams', function() use ($active_teams) {...})
  - Inactive teams group: render_collection_group('Inaktive Teams', function() use ($inactive_teams) {...})
  - Empty state if no teams: render_empty('people-fill', 'Noch keine Teams', 'Erstelle das erste Team.').
  - Team status badges: render_badge('ok', 'Aktiv'), render_badge('dim', 'Inaktiv').

  **team_create.php specific changes:**
  - Form for creating a new team (if this is a separate template from team_form.php).
  - Standard checklist: remove inline styles, fix form-control.
  - If team name input: form-control without -sm.

  **team_edit.php specific changes:**
  - Form for editing an existing team.
  - Standard checklist.
  - May have a danger zone for team deletion/deactivation → use render_danger_zone().

  **team_form.php specific changes:**
  - Shared form partial for team name/settings fields (if used as include).
  - Standard checklist. Ensure form-control without -sm.

  **coach_form.php specific changes:**
  - Form for creating a coordinator (username, password, email, team assignment).
  - All inputs: form-control without -sm.
  - If using card sections, remove shadow-sm.
  - Email input: type="email" form-control.
  </action>

  <verify>
    <automated>
    php -l /Users/sebastianwiller/Documents/github/team-manager/src/templates/admin/dashboard.php && \
    php -l /Users/sebastianwiller/Documents/github/team-manager/src/templates/admin/team_create.php && \
    php -l /Users/sebastianwiller/Documents/github/team-manager/src/templates/admin/team_edit.php && \
    php -l /Users/sebastianwiller/Documents/github/team-manager/src/templates/admin/team_form.php && \
    php -l /Users/sebastianwiller/Documents/github/team-manager/src/templates/admin/coach_form.php
    # All must exit 0

    for f in dashboard team_create team_edit team_form coach_form; do
      count=$(grep -c 'style="' /Users/sebastianwiller/Documents/github/team-manager/src/templates/admin/${f}.php 2>/dev/null || echo 0)
      onerror=$(grep -c 'onerror' /Users/sebastianwiller/Documents/github/team-manager/src/templates/admin/${f}.php 2>/dev/null || echo 0)
      echo "admin/$f: $((count - onerror)) violations"
    done
    # All must show: 0 violations

    grep -c "render_collection_group\|render_empty" /Users/sebastianwiller/Documents/github/team-manager/src/templates/admin/dashboard.php
    # Must output: >= 2
    </automated>
  </verify>

  <acceptance_criteria>
  - All 5 files pass `php -l`
  - Zero `style="` violations (excluding onerror) in all 5 files
  - dashboard.php contains `render_collection_group(` and `render_empty(`
  - coach_form.php has no `form-control-sm`
  - team_edit.php has `render_danger_zone(` if a delete/deactivate danger zone exists
  </acceptance_criteria>

  <done>5 admin team management templates migrated. Dashboard uses render_collection_group and render_empty partials.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate coordinator_settings.php, coordinator_edit_email.php, columns.php, column_delete_confirm.php, column_merge_confirm.php, column_rename.php</name>
  <files>
    src/templates/admin/coordinator_settings.php,
    src/templates/admin/coordinator_edit_email.php,
    src/templates/admin/columns.php,
    src/templates/admin/column_delete_confirm.php,
    src/templates/admin/column_merge_confirm.php,
    src/templates/admin/column_rename.php
  </files>

  <read_first>
  - src/templates/admin/coordinator_settings.php — read ENTIRE file
  - src/templates/admin/coordinator_edit_email.php — read ENTIRE file
  - src/templates/admin/columns.php — read ENTIRE file
  - src/templates/admin/column_delete_confirm.php — read ENTIRE file
  - src/templates/admin/column_merge_confirm.php — read ENTIRE file
  - src/templates/admin/column_rename.php — read ENTIRE file
  - src/templates/components/partials.php — review signatures
  </read_first>

  <action>
  Apply the standard violation checklist to all 6 files.

  **coordinator_settings.php specific changes:**
  - Admin view of coordinator settings (manage coordinator, may include coordinator detail).
  - Standard checklist. May have a danger zone for coordinator deactivation → render_danger_zone().
  - ?success=1 flash at top.

  **coordinator_edit_email.php specific changes:**
  - Short form to edit coordinator email. Email input: form-control without -sm.
  - Wrap in render_form_section('E-Mail-Adresse', ...) if currently in a card.

  **columns.php (admin) specific changes:**
  - Admin view of global columns (team-wide EAV structure).
  - Column type badges: render_badge('dim', 'Ja/Nein'), render_badge('dim', 'Zahl'), render_badge('dim', 'Text').
  - Coordinator-only badge: render_badge('bad', 'K') (red per UI-SPEC).
  - Global badge: render_badge('dim', 'G').
  - Empty state: render_empty('table', 'Noch keine Spalten', 'Füge die erste globale Spalte hinzu.').

  **column_delete_confirm.php specific changes (Bestätigungsseite archetype):**
  - Page heading: "Möchtest du die Spalte [name] wirklich löschen?"
  - Body: "Diese Aktion kann nicht rückgängig gemacht werden."
  - Confirm: `<button type="submit" class="btn btn-danger w-100 mb-2">Ja, löschen</button>`
  - Cancel: `<a href="/admin/columns" class="btn btn-outline-secondary w-100">Abbrechen</a>`
  - NO JavaScript confirm().
  - Remove any inline styles.

  **column_merge_confirm.php specific changes (Bestätigungsseite archetype):**
  - Same Bestätigungsseite pattern as column_delete_confirm.php.
  - Heading: "Möchtest du diese Spalten wirklich zusammenführen?"
  - Confirm button: btn-danger, cancel: btn-outline-secondary or text link.
  - No JS confirm().

  **column_rename.php specific changes:**
  - Form to rename a global column. Text input: form-control without -sm.
  - Standard checklist.
  </action>

  <verify>
    <automated>
    php -l /Users/sebastianwiller/Documents/github/team-manager/src/templates/admin/coordinator_settings.php && \
    php -l /Users/sebastianwiller/Documents/github/team-manager/src/templates/admin/coordinator_edit_email.php && \
    php -l /Users/sebastianwiller/Documents/github/team-manager/src/templates/admin/columns.php && \
    php -l /Users/sebastianwiller/Documents/github/team-manager/src/templates/admin/column_delete_confirm.php && \
    php -l /Users/sebastianwiller/Documents/github/team-manager/src/templates/admin/column_merge_confirm.php && \
    php -l /Users/sebastianwiller/Documents/github/team-manager/src/templates/admin/column_rename.php
    # All must exit 0

    grep -c "btn-danger" /Users/sebastianwiller/Documents/github/team-manager/src/templates/admin/column_delete_confirm.php
    # Must output: >= 1

    grep -c "btn-danger" /Users/sebastianwiller/Documents/github/team-manager/src/templates/admin/column_merge_confirm.php
    # Must output: >= 1

    grep -n 'style="' /Users/sebastianwiller/Documents/github/team-manager/src/templates/admin/columns.php | grep -v "onerror"
    # Must produce NO output
    </automated>
  </verify>

  <acceptance_criteria>
  - All 6 files pass `php -l`
  - Zero `style="` violations (excluding onerror) in all 6 files
  - column_delete_confirm.php contains `btn-danger`
  - column_merge_confirm.php contains `btn-danger`
  - Neither confirm file has `confirm(` (no JS confirm dialog)
  - columns.php contains `render_badge(`
  - coordinator_edit_email.php has no `form-control-sm` on email input
  </acceptance_criteria>

  <done>6 admin coordinator/column templates migrated. Confirmation pages follow Bestätigungsseite archetype.</done>
</task>

</tasks>

<verification>
Final check across all 11 admin templates in this plan:
```bash
grep -rn 'style="' /Users/sebastianwiller/Documents/github/team-manager/src/templates/admin/ \
  --include="*.php" \
  --exclude="credential_modal.php" \
  --exclude="members.php" --exclude="member_form.php" --exclude="member_edit.php" \
  --exclude="clubs.php" --exclude="club_form.php" --exclude="club_edit.php" \
  --exclude="attributes.php" --exclude="notify_coordinators.php" | grep -v "onerror"
```
Should produce NO output.

Confirm credential_modal.php untouched:
```bash
git -C /Users/sebastianwiller/Documents/github/team-manager diff --name-only src/templates/admin/credential_modal.php
```
Should produce NO output.
</verification>

<success_criteria>
- All 11 admin templates in this plan pass php -l
- Zero inline style violations across all 11 files
- Zero spacing violations (py-5, mb-5) across all 11 files
- Confirmation pages use btn-danger + cancel link (no JS confirm)
- credential_modal.php is untouched
- render_empty, render_collection_group, render_badge used where applicable
</success_criteria>

<output>
After completion, create `.planning/phases/09-ui-vereinheitlichung/09-07-SUMMARY.md`
</output>
