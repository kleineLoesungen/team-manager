---
phase: 09-ui-vereinheitlichung
plan: 06
type: execute
wave: 3
depends_on: [09-02]
files_modified:
  - src/templates/member/lists.php
  - src/templates/member/list_detail.php
  - src/templates/member/list_row_form.php
  - src/templates/member/ticker_list.php
  - src/templates/member/ticker_detail.php
  - src/templates/member/file_detail.php
  - src/templates/member/stats.php
  - src/templates/member/profile.php
  - src/templates/member/member_profile.php
  - src/templates/member/coordinators.php
  - src/templates/member/confirm_profile.php
autonomous: true
requirements: []

must_haves:
  truths:
    - "All 11 member templates have zero inline style= violations"
    - "All 11 member templates have zero py-5, mb-5 spacing violations"
    - "member/lists.php uses render_empty() and render_badge() for empty state and visibility"
    - "member/list_detail.php uses render_matrix_table() for the EAV cell table"
    - "member/stats.php uses render_matrix_table() for the stats table"
    - "member/profile.php: email input uses form-control (no -sm)"
  artifacts:
    - path: "src/templates/member/lists.php"
      provides: "Member list overview with partials for empty state, badges, groups"
    - path: "src/templates/member/list_detail.php"
      provides: "Member cell view with render_matrix_table"
    - path: "src/templates/member/stats.php"
      provides: "Member stats with render_matrix_table"
  key_links:
    - from: "src/templates/member/list_detail.php"
      to: "render_matrix_table()"
      via: "wraps the member-visible columns table"
      pattern: "render_matrix_table"
    - from: "src/templates/member/lists.php"
      to: "render_badge()"
      via: "visibility badges on each list item"
      pattern: "render_badge"
---

<objective>
Migrate all 11 member role templates.

Purpose: The member role has 11 templates (lists, list detail, row edit, ticker, file, stats,
profile, player profile, coordinator info, confirmation). These are simpler than coordinator
templates but must follow the same violation checklist.

Output: 11 member templates migrated, zero violations, partials used consistently.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-ui-vereinheitlichung/09-UI-SPEC.md
@.planning/phases/09-ui-vereinheitlichung/09-RESEARCH.md
@.planning/UI-BASELINE.md
@.planning/phases/09-ui-vereinheitlichung/09-02-SUMMARY.md
</context>

<interfaces>
<!-- Partials available everywhere — no require_once needed: -->
render_flash(string $type, string $message): void
render_empty(string $icon, string $heading, string $body_text, ?string $action_html): void
render_badge(string $type, string $label): void
render_collection_group(string $label, callable $items_body): void
render_form_section(string $heading, callable $fields_body, ?string $footer_html): void
render_form_field(string $label, string $input_html, ?string $hint): void
render_matrix_table(array $columns, callable $rows_body, ?callable $footer_body): void
render_action_bar(string $label, string $form_id): void
render_page_header(string $title, ?string $back_url, ?string $action_html): void
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Migrate lists.php, list_detail.php, list_row_form.php, ticker_list.php, ticker_detail.php, file_detail.php</name>
  <files>
    src/templates/member/lists.php,
    src/templates/member/list_detail.php,
    src/templates/member/list_row_form.php,
    src/templates/member/ticker_list.php,
    src/templates/member/ticker_detail.php,
    src/templates/member/file_detail.php
  </files>

  <read_first>
  - src/templates/member/lists.php — read ENTIRE file
  - src/templates/member/list_detail.php — read ENTIRE file
  - src/templates/member/list_row_form.php — read ENTIRE file
  - src/templates/member/ticker_list.php — read ENTIRE file
  - src/templates/member/ticker_detail.php — read ENTIRE file
  - src/templates/member/file_detail.php — read ENTIRE file
  - src/templates/components/partials.php — review signatures
  </read_first>

  <action>
  Apply the standard violation checklist to all 6 files:
  - Remove all `style="..."` attributes (except img onerror)
  - Replace `style="width:3em;height:1.75em;cursor:pointer;"` on switch inputs → remove style attr
  - Replace py-5 empty states → render_empty()
  - Replace mb-5 → mb-4; remove *-0 and *-1 spacing utilities
  - Remove shadow-sm from cards, max-width inline styles
  - Replace form-control-sm → form-control (remove -sm suffix)
  - Replace badge inline patterns → render_badge()
  - Add ?success=1 flash check at top of body

  **lists.php (member) specific changes:**
  - Member's list overview (shows public + protected lists for their team).
  - Visibility badges: render_badge('ok', 'Öffentlich'), render_badge('warn', 'Geschützt').
  - Empty state: render_empty('collection', 'Noch keine Listen', 'Der Koordinator hat noch keine Listen angelegt.').
  - If lists are grouped by date, use render_collection_group() for each date group.
  - The calendar/list view switcher (seg-ctrl) should remain as-is — it uses .seg-ctrl class
    which is defined in the CSS (moved from inline block). Remove any inline styles on it.

  **list_detail.php (member) specific changes:**
  - Member sees the list table with their own row highlighted or editable.
  - This also uses a matrix-style table. Wrap with render_matrix_table():
    ```php
    <?php render_matrix_table($column_headers, function() use ($rows) {
        foreach ($rows as $row) { ?>
        <tr class="<?= $row['is_own'] ? 'table-active' : '' ?>">
            ...
        </tr>
        <?php }
    }); ?>
    ```
  - Switch inputs for boolean cells: remove inline style.
  - Number inputs: add cell-number class.
  - Text inputs: add cell-text class.

  **list_row_form.php (member) specific changes:**
  - Member's own-row edit form. Similar to coordinator's list_row_form.php.
  - Remove inline styles from switch inputs.
  - Number inputs → cell-number class; text inputs → cell-text class.
  - If sticky save button, use render_action_bar('Zeile speichern', 'row-form').

  **ticker_list.php (member) specific changes:**
  - Member's ticker overview (lists tickers they can access).
  - Status badges: render_badge('ok', 'Aktiv'), render_badge('dim', 'Beendet').
  - Empty state: render_empty('megaphone', 'Noch keine Ticker', 'Aktive Ticker erscheinen hier.').

  **ticker_detail.php (member) specific changes:**
  - Member ticker view with message list. If member has write access, includes post form.
  - Message form textarea: form-control without -sm.
  - Tag select: form-select without -sm.

  **file_detail.php (member) specific changes:**
  - Markdown file detail view for members.
  - Standard checklist. No form-control-sm on any visible inputs.
  - Rendered Markdown content area: no inline styles needed (uses prose text naturally).
  </action>

  <verify>
    <automated>
    php -l /Users/sebastianwiller/Documents/github/team-manager/src/templates/member/lists.php && \
    php -l /Users/sebastianwiller/Documents/github/team-manager/src/templates/member/list_detail.php && \
    php -l /Users/sebastianwiller/Documents/github/team-manager/src/templates/member/list_row_form.php && \
    php -l /Users/sebastianwiller/Documents/github/team-manager/src/templates/member/ticker_list.php && \
    php -l /Users/sebastianwiller/Documents/github/team-manager/src/templates/member/ticker_detail.php && \
    php -l /Users/sebastianwiller/Documents/github/team-manager/src/templates/member/file_detail.php
    # All must exit 0

    for f in lists list_detail list_row_form ticker_list ticker_detail file_detail; do
      count=$(grep -c 'style="' /Users/sebastianwiller/Documents/github/team-manager/src/templates/member/${f}.php 2>/dev/null || echo 0)
      onerror=$(grep -c 'onerror' /Users/sebastianwiller/Documents/github/team-manager/src/templates/member/${f}.php 2>/dev/null || echo 0)
      echo "member/$f: $((count - onerror)) violations"
    done
    # All must show: 0 violations

    grep -c "render_matrix_table" /Users/sebastianwiller/Documents/github/team-manager/src/templates/member/list_detail.php
    # Must output: >= 1

    grep -c "render_badge\|render_empty" /Users/sebastianwiller/Documents/github/team-manager/src/templates/member/lists.php
    # Must output: >= 2
    </automated>
  </verify>

  <acceptance_criteria>
  - All 6 files pass `php -l`
  - Zero `style="` violations (excluding onerror) in all 6 files
  - Zero `py-5`, `mb-5`, `shadow-sm` in all 6 files
  - member/lists.php contains `render_badge(` and `render_empty(`
  - member/list_detail.php contains `render_matrix_table(`
  - member/list_row_form.php has no `style="width:3em` on switch inputs
  - member/ticker_list.php contains `render_empty(`
  </acceptance_criteria>

  <done>6 member templates migrated. List detail uses render_matrix_table. Lists overview uses render_badge and render_empty.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate stats.php, profile.php, member_profile.php, coordinators.php, confirm_profile.php</name>
  <files>
    src/templates/member/stats.php,
    src/templates/member/profile.php,
    src/templates/member/member_profile.php,
    src/templates/member/coordinators.php,
    src/templates/member/confirm_profile.php
  </files>

  <read_first>
  - src/templates/member/stats.php — read ENTIRE file (has stats table)
  - src/templates/member/profile.php — read ENTIRE file (has email form field)
  - src/templates/member/member_profile.php — read ENTIRE file (player profile view)
  - src/templates/member/coordinators.php — read ENTIRE file
  - src/templates/member/confirm_profile.php — read ENTIRE file
  - src/templates/components/partials.php — review signatures
  </read_first>

  <action>
  Apply the standard violation checklist to all 5 files:
  - Remove all `style="..."` attributes (except img onerror)
  - Replace py-5 empty states → render_empty()
  - Replace mb-5 → mb-4; remove *-0, *-1 spacing utilities
  - Remove shadow-sm from cards
  - Replace form-control-sm → form-control (remove -sm suffix)
  - Replace badge inline patterns → render_badge()
  - Add ?success=1 flash check at top of body

  **stats.php (member) specific changes:**
  - Member's own stats table (shows only their own aggregated row).
  - The stats table should use render_matrix_table():
    ```php
    <?php render_matrix_table($column_headers, function() use ($stats_row) { ?>
        <tr>
            <?php foreach ($stats_row as $val): ?>
            <td><?= e($val) ?></td>
            <?php endforeach; ?>
        </tr>
    <?php }); ?>
    ```
  - Per UI-SPEC: "No player name column in player stats table" — member always views own row.
  - Remove any form-control-sm from filter inputs if present.

  **profile.php (member) specific changes:**
  - Member's own profile page where they can set their email address.
  - Email input: `<input type="email" class="form-control" name="email" ...>` — no -sm.
  - Form wrapper card: if using shadow-sm, remove it. Use render_form_section() if appropriate.
  - Success flash at top: when email is saved, show render_flash('success', 'Gespeichert.').

  **member_profile.php (member) specific changes:**
  - Member's view of their player profile with attribute groups.
  - Attribute values shown as read-only text or form inputs (if editable_by_player=true).
  - Remove inline styles from attribute value display.
  - Attribute group cards: remove shadow-sm.
  - Editable attributes with switch: remove inline style.

  **coordinators.php (member) specific changes:**
  - Member's view showing their team coordinator(s).
  - Simple list view. Standard checklist.
  - Empty state if no coordinators: render_empty('person-badge', 'Kein Koordinator', 'Noch kein Koordinator für dieses Team.').

  **confirm_profile.php (member) specific changes:**
  - Confirmation page for linking a player profile or similar action.
  - If this is a Bestätigungsseite pattern:
    - Heading: appropriate "Möchtest du ... wirklich ...?" heading
    - btn-danger for confirm, cancel link/btn-outline-secondary for cancel
    - No JS confirm
  - Standard checklist: remove inline styles.
  </action>

  <verify>
    <automated>
    php -l /Users/sebastianwiller/Documents/github/team-manager/src/templates/member/stats.php && \
    php -l /Users/sebastianwiller/Documents/github/team-manager/src/templates/member/profile.php && \
    php -l /Users/sebastianwiller/Documents/github/team-manager/src/templates/member/member_profile.php && \
    php -l /Users/sebastianwiller/Documents/github/team-manager/src/templates/member/coordinators.php && \
    php -l /Users/sebastianwiller/Documents/github/team-manager/src/templates/member/confirm_profile.php
    # All must exit 0

    for f in stats profile member_profile coordinators confirm_profile; do
      count=$(grep -c 'style="' /Users/sebastianwiller/Documents/github/team-manager/src/templates/member/${f}.php 2>/dev/null || echo 0)
      onerror=$(grep -c 'onerror' /Users/sebastianwiller/Documents/github/team-manager/src/templates/member/${f}.php 2>/dev/null || echo 0)
      echo "member/$f: $((count - onerror)) violations"
    done
    # All must show: 0 violations

    grep -c "render_matrix_table" /Users/sebastianwiller/Documents/github/team-manager/src/templates/member/stats.php
    # Must output: >= 1

    grep -c "form-control" /Users/sebastianwiller/Documents/github/team-manager/src/templates/member/profile.php
    grep -c "form-control-sm" /Users/sebastianwiller/Documents/github/team-manager/src/templates/member/profile.php
    # Second command must output: 0
    </automated>
  </verify>

  <acceptance_criteria>
  - All 5 files pass `php -l`
  - Zero `style="` violations (excluding onerror) in all 5 files
  - Zero `py-5`, `mb-5`, `shadow-sm` in all 5 files
  - member/stats.php contains `render_matrix_table(`
  - member/profile.php has no `form-control-sm` on the email input
  - member/coordinators.php has no inline styles
  </acceptance_criteria>

  <done>5 member account/stats templates migrated. Stats page uses render_matrix_table. Profile form has correct form-control sizing.</done>
</task>

</tasks>

<verification>
Run across all 11 member templates:
```bash
grep -rn 'style="' /Users/sebastianwiller/Documents/github/team-manager/src/templates/member/ | grep -v "onerror"
```
Should produce NO output.

```bash
grep -rn 'py-5\|mb-5\|shadow-sm\|form-control-sm\|form-select-sm' /Users/sebastianwiller/Documents/github/team-manager/src/templates/member/
```
Should produce NO output.
</verification>

<success_criteria>
- All 11 member role templates pass php -l
- Zero inline style violations across all 11 files
- Zero spacing violations (py-5, mb-5) across all 11 files
- Zero form-control-sm violations across all 11 files
- render_matrix_table used in list_detail.php and stats.php
- render_badge and render_empty used in lists.php
</success_criteria>

<output>
After completion, create `.planning/phases/09-ui-vereinheitlichung/09-06-SUMMARY.md`
</output>
