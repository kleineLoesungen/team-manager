---
phase: 07-live-ticker
plan: 03
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - src/coordinator/ticker_handler.php
  - src/coordinator/ticker_create_handler.php
  - src/coordinator/ticker_detail_handler.php
  - src/coordinator/ticker_close_handler.php
  - src/coordinator/ticker_delete_handler.php
  - src/templates/coordinator/ticker.php
  - src/templates/coordinator/ticker_form.php
  - src/templates/coordinator/ticker_detail.php
  - src/templates/coordinator/ticker_delete_confirm.php
autonomous: true
requirements:
  - TICKER-01
  - TICKER-02
  - TICKER-03

must_haves:
  truths:
    - "Coordinator sees list of their tickers at /coordinator/ticker"
    - "Coordinator can create ticker with name, description, freigabe member checkboxes (D-11)"
    - "Coordinator can post messages (max 280 chars, timestamp, optional tag) via ticker detail page"
    - "Coordinator can edit and delete any message in the ticker (D-12)"
    - "Coordinator can close ticker (status → closed) via close handler"
    - "Coordinator can delete ticker via two-step confirm (GET shows confirm, POST deletes)"
    - "All POST forms include CSRF token; all redirect back (PRG)"
  artifacts:
    - path: "src/coordinator/ticker_handler.php"
      provides: "GET /coordinator/ticker — list of team tickers"
      contains: "require_coordinator()"
    - path: "src/coordinator/ticker_create_handler.php"
      provides: "GET/POST /coordinator/ticker/new — create form + INSERT"
      contains: "ticker_members"
    - path: "src/coordinator/ticker_detail_handler.php"
      provides: "GET/POST /coordinator/ticker/{id} — detail view + message CRUD"
      contains: "mb_strlen"
    - path: "src/coordinator/ticker_close_handler.php"
      provides: "POST /coordinator/ticker/{id}/close — set status=closed"
      contains: "status.*closed"
    - path: "src/coordinator/ticker_delete_handler.php"
      provides: "GET shows confirm page; POST deletes ticker + cascades messages"
      contains: "ticker_delete_confirm"
    - path: "src/templates/coordinator/ticker.php"
      provides: "Coordinator ticker list with status badges"
      contains: "bg-success"
    - path: "src/templates/coordinator/ticker_form.php"
      provides: "Ticker create form with freigabe checkboxes"
      contains: "ticker_members"
    - path: "src/templates/coordinator/ticker_detail.php"
      provides: "Ticker detail: message feed (newest first) + post form + edit/delete per message"
      contains: "mb_strlen\|280\|timestamp"
    - path: "src/templates/coordinator/ticker_delete_confirm.php"
      provides: "Two-step delete confirmation page"
      contains: "dauerhaft gelöscht"
  key_links:
    - from: "src/coordinator/ticker_detail_handler.php"
      to: "team_manager.tickers (PostgreSQL)"
      via: "WHERE id = ? AND team_id = $_SESSION['team_id']"
      pattern: "team_id.*_SESSION"
    - from: "src/coordinator/ticker_create_handler.php"
      to: "team_manager.ticker_members"
      via: "INSERT batch for selected member checkboxes"
      pattern: "ticker_members"
    - from: "src/templates/coordinator/ticker_detail.php"
      to: "POST /coordinator/ticker/{id}"
      via: "form action for post_message, edit_message, delete_message actions"
      pattern: "action.*post_message\|action.*delete_message"
---

<objective>
Create all coordinator-side ticker handlers and templates: ticker list, create/edit form (with freigabe member selection), ticker detail (message feed + post form + message edit/delete), close action, and two-step delete.

Purpose: Coordinators manage the full lifecycle of a ticker and control who can post (TICKER-01, TICKER-02, TICKER-03).
Output: 5 PHP handlers + 4 PHP templates in coordinator namespaces.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-live-ticker/07-CONTEXT.md
@.planning/phases/07-live-ticker/07-RESEARCH.md
@.planning/phases/07-live-ticker/07-UI-SPEC.md

<interfaces>
<!-- render_coach_page (from src/templates/coordinator/layout.php) -->
```php
function render_coach_page(string $title, string $active, callable $body): void
// Use $active = 'ticker' for all ticker pages (Plan 07-05 adds the nav item)
```

<!-- require_coordinator() (from src/auth/session.php) -->
```php
function require_coordinator(): void
// Redirects to /login if not authenticated as coordinator
// Sets up $_SESSION['team_id'], $_SESSION['user_id'], $_SESSION['role']
```

<!-- require_csrf() (from src/utils/csrf.php) -->
```php
// Call require_csrf() at top of POST blocks to validate token
// csrf_field() generates the hidden input in templates
```

<!-- Session values after require_coordinator() -->
// $_SESSION['team_id']  — coordinator's team
// $_SESSION['user_id']  — coordinator's user id
// $_SESSION['role']     — always 'coordinator' at this point

<!-- redirect() (from src/utils/helpers.php) -->
```php
function redirect(string $url): never
```

<!-- e() (from src/utils/helpers.php) -->
```php
function e(mixed $value): string  // htmlspecialchars wrapper
```

<!-- Pitfall from RESEARCH.md: use mb_strlen($message, 'UTF-8') not strlen() for 280-char check -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ticker list handler + create handler + list template + create/edit form template</name>
  <files>
    src/coordinator/ticker_handler.php,
    src/coordinator/ticker_create_handler.php,
    src/templates/coordinator/ticker.php,
    src/templates/coordinator/ticker_form.php
  </files>
  <read_first>
    - src/coordinator/lists_handler.php — read FULL file (pattern for simple list handler)
    - src/coordinator/list_create_handler.php — read FULL file (pattern for create handler with PRG)
    - src/templates/coordinator/lists.php — read FULL file (pattern for list template with status badges)
    - src/coordinator/members_handler.php — read lines 1–40 (pattern for fetching members for a team)
  </read_first>
  <action>
Create 4 files:

### 1. src/coordinator/ticker_handler.php
Fetches all tickers for the coordinator's team, renders the list template.

```php
<?php
// src/coordinator/ticker_handler.php — GET /coordinator/ticker

declare(strict_types=1);

require_coordinator();

$pdo = get_db();

$stmt = $pdo->prepare(
    "SELECT id, name, description, status, created_at
     FROM tickers
     WHERE team_id = ?
     ORDER BY (status = 'active') DESC, created_at DESC"
);
$stmt->execute([$_SESSION['team_id']]);
$tickers = $stmt->fetchAll(PDO::FETCH_ASSOC);

$success = !empty($_GET['success']) ? match($_GET['success']) {
    'created'   => 'Ticker angelegt.',
    'closed'    => 'Ticker geschlossen.',
    'deleted'   => 'Ticker gelöscht.',
    default     => '',
} : '';

require ROOT_PATH . '/src/templates/coordinator/layout.php';

render_coach_page('Ticker', 'ticker', function() use ($tickers, $success) {
    if ($success) echo '<div class="alert alert-success">' . e($success) . '</div>';
    require ROOT_PATH . '/src/templates/coordinator/ticker.php';
});
```

### 2. src/coordinator/ticker_create_handler.php
Handles GET (show create form) and POST (insert ticker + ticker_members).

```php
<?php
// src/coordinator/ticker_create_handler.php — GET/POST /coordinator/ticker/new

declare(strict_types=1);

require_coordinator();

$pdo = get_db();

// Fetch team members for freigabe checkboxes (D-11)
$stmt = $pdo->prepare(
    "SELECT id, first_name, last_name
     FROM users
     WHERE team_id = ? AND role = 'member' AND is_active = TRUE
     ORDER BY first_name, last_name"
);
$stmt->execute([$_SESSION['team_id']]);
$members = $stmt->fetchAll(PDO::FETCH_ASSOC);

$error = '';
$form_values = ['name' => '', 'description' => '', 'freigabe_members' => []];

if ($_SERVER['REQUEST_METHOD'] === 'POST') {
    require_csrf();

    $name        = trim($_POST['name'] ?? '');
    $description = trim($_POST['description'] ?? '');
    $freigabe    = isset($_POST['freigabe_members']) && is_array($_POST['freigabe_members'])
                   ? array_map('intval', $_POST['freigabe_members'])
                   : [];

    if ($name === '' || mb_strlen($name, 'UTF-8') > 255) {
        $error = 'Ticker-Name ist erforderlich (max. 255 Zeichen).';
        $form_values = compact('name', 'description', 'freigabe');
    } else {
        $pdo->beginTransaction();
        try {
            $stmt = $pdo->prepare(
                "INSERT INTO tickers (team_id, name, description, status, created_at, updated_at)
                 VALUES (?, ?, ?, 'active', NOW(), NOW())
                 RETURNING id"
            );
            $stmt->execute([$_SESSION['team_id'], $name, $description ?: null]);
            $ticker_id = (int)$stmt->fetchColumn();

            // Insert freigabe members (D-11)
            if (!empty($freigabe)) {
                $insert = $pdo->prepare(
                    "INSERT INTO ticker_members (ticker_id, user_id, team_id)
                     VALUES (?, ?, ?)
                     ON CONFLICT DO NOTHING"
                );
                foreach ($freigabe as $member_id) {
                    $insert->execute([$ticker_id, $member_id, $_SESSION['team_id']]);
                }
            }

            $pdo->commit();
            redirect('/coordinator/ticker/' . $ticker_id);
        } catch (\Throwable $e) {
            $pdo->rollBack();
            $error = 'Fehler beim Anlegen des Tickers. Bitte versuche es erneut.';
            error_log('ticker_create_handler: ' . $e->getMessage());
        }
    }
}

require ROOT_PATH . '/src/templates/coordinator/layout.php';

render_coach_page('Neuer Ticker', 'ticker', function() use ($members, $error, $form_values) {
    if ($error) echo '<div class="alert alert-danger">' . e($error) . '</div>';
    require ROOT_PATH . '/src/templates/coordinator/ticker_form.php';
});
```

### 3. src/templates/coordinator/ticker.php
List of all team tickers with status badges and action links.

```php
<?php
// src/templates/coordinator/ticker.php — coordinator ticker list
// Variables: $tickers (array)
?>
<div class="d-flex justify-content-between align-items-center mb-4">
    <span class="text-muted"><?= count($tickers) ?> <?= count($tickers) === 1 ? 'Ticker' : 'Ticker' ?></span>
    <a href="/coordinator/ticker/new" class="btn btn-primary btn-sm">
        <i class="bi bi-plus-lg me-1"></i>Neuer Ticker
    </a>
</div>

<?php if (empty($tickers)): ?>
<div class="text-center py-5">
    <p class="h5 text-muted">Noch keine Ticker erstellt</p>
    <p class="text-muted">Du kannst einen neuen Ticker anlegen, um Events zu dokumentieren.</p>
    <a href="/coordinator/ticker/new" class="btn btn-primary mt-2">+ Neuer Ticker</a>
</div>
<?php else: ?>
<div class="list-group mb-4">
    <?php foreach ($tickers as $t): ?>
    <div class="list-group-item list-group-item-action d-flex justify-content-between align-items-start py-3">
        <div class="flex-grow-1">
            <div class="d-flex align-items-center gap-2 mb-1">
                <a href="/coordinator/ticker/<?= (int)$t['id'] ?>" class="fw-semibold text-decoration-none">
                    <?= e($t['name']) ?>
                </a>
                <?php if ($t['status'] === 'active'): ?>
                    <span class="badge bg-success">Aktiv</span>
                <?php else: ?>
                    <span class="badge bg-secondary">Geschlossen</span>
                <?php endif; ?>
            </div>
            <?php if ($t['description']): ?>
            <p class="mb-0 text-muted small"><?= e($t['description']) ?></p>
            <?php endif; ?>
            <p class="mb-0 text-muted small mt-1">
                <i class="bi bi-clock me-1"></i><?= e(date('d.m.Y H:i', strtotime($t['created_at']))) ?>
            </p>
        </div>
        <div class="d-flex gap-2 ms-3 flex-shrink-0">
            <a href="/coordinator/ticker/<?= (int)$t['id'] ?>" class="btn btn-sm btn-outline-secondary">Ansehen</a>
            <?php if ($t['status'] === 'active'): ?>
            <form method="POST" action="/coordinator/ticker/<?= (int)$t['id'] ?>/close" class="d-inline">
                <?= csrf_field() ?>
                <button type="submit" class="btn btn-sm btn-outline-warning">Schließen</button>
            </form>
            <?php endif; ?>
            <a href="/coordinator/ticker/<?= (int)$t['id'] ?>/delete" class="btn btn-sm btn-outline-danger">Löschen</a>
        </div>
    </div>
    <?php endforeach; ?>
</div>
<?php endif; ?>
```

### 4. src/templates/coordinator/ticker_form.php
Create form with ticker name, description, member freigabe checkboxes.

```php
<?php
// src/templates/coordinator/ticker_form.php — create ticker form
// Variables: $members (array), $form_values (array with keys: name, description, freigabe_members)
$freigabe_ids = $form_values['freigabe_members'] ?? [];
?>
<form method="POST" action="/coordinator/ticker/new">
    <?= csrf_field() ?>

    <div class="mb-3">
        <label for="ticker_name" class="form-label fw-semibold">Ticker-Name <span class="text-danger">*</span></label>
        <input type="text" id="ticker_name" name="name" class="form-control"
               maxlength="255" required
               value="<?= e($form_values['name'] ?? '') ?>"
               placeholder='z.B. "Finale 2026"'>
        <div class="form-text">Erforderlich. Max. 255 Zeichen.</div>
    </div>

    <div class="mb-4">
        <label for="ticker_description" class="form-label">Beschreibung</label>
        <textarea id="ticker_description" name="description" class="form-control" rows="2"
                  placeholder='z.B. "Live-Ticker vom Turnier in München"'><?= e($form_values['description'] ?? '') ?></textarea>
        <div class="form-text">Optional. Kurze Info für die Besucher des Tickers.</div>
    </div>

    <?php if (!empty($members)): ?>
    <div class="mb-4">
        <label class="form-label fw-semibold">Mitglieder freigeben</label>
        <p class="form-text mt-0 mb-2">Folgende Mitglieder dürfen Nachrichten posten:</p>
        <div class="border rounded p-3" style="max-height: 200px; overflow-y: auto;">
            <?php foreach ($members as $m): ?>
            <div class="form-check">
                <input class="form-check-input" type="checkbox"
                       name="freigabe_members[]"
                       value="<?= (int)$m['id'] ?>"
                       id="m_<?= (int)$m['id'] ?>"
                       <?= in_array((int)$m['id'], $freigabe_ids, true) ? 'checked' : '' ?>>
                <label class="form-check-label" for="m_<?= (int)$m['id'] ?>">
                    <?= e($m['first_name'] . ' ' . $m['last_name']) ?>
                </label>
            </div>
            <?php endforeach; ?>
        </div>
        <div class="form-text mt-1">Der Koordinator kann immer posten.</div>
    </div>
    <?php endif; ?>

    <div class="d-flex gap-2">
        <button type="submit" class="btn btn-primary">Speichern</button>
        <a href="/coordinator/ticker" class="btn btn-outline-secondary">Abbrechen</a>
    </div>
</form>
```
  </action>
  <verify>
    <automated>php -l /Users/sebastianwiller/Documents/github/team-manager/src/coordinator/ticker_handler.php && php -l /Users/sebastianwiller/Documents/github/team-manager/src/coordinator/ticker_create_handler.php && php -l /Users/sebastianwiller/Documents/github/team-manager/src/templates/coordinator/ticker.php && php -l /Users/sebastianwiller/Documents/github/team-manager/src/templates/coordinator/ticker_form.php</automated>
  </verify>
  <done>All 4 files exist and pass php -l. ticker_handler.php queries tickers by team_id ordered by active first. ticker_create_handler.php handles POST with freigabe_members array and uses beginTransaction/commit. ticker.php shows empty state + list with status badges. ticker_form.php has freigabe checkboxes and validates name.</done>
</task>

<task type="auto">
  <name>Task 2: Ticker detail handler + close handler + delete handler + detail template + delete confirm template</name>
  <files>
    src/coordinator/ticker_detail_handler.php,
    src/coordinator/ticker_close_handler.php,
    src/coordinator/ticker_delete_handler.php,
    src/templates/coordinator/ticker_detail.php,
    src/templates/coordinator/ticker_delete_confirm.php
  </files>
  <read_first>
    - src/coordinator/list_delete_handler.php — read FULL file (two-step delete confirm pattern)
    - src/coordinator/list_detail_handler.php — read FULL file (pattern for detail handler with inline POST actions)
    - src/templates/coordinator/list_detail.php — read lines 1–60 (pattern for detail template)
  </read_first>
  <action>
Create 5 files:

### 1. src/coordinator/ticker_detail_handler.php
GET: show ticker detail with message feed + post form.
POST action=post_message: insert message.
POST action=delete_message: delete message (coordinator can delete any).
POST action=edit_message: update message text/timestamp/tag.

```php
<?php
// src/coordinator/ticker_detail_handler.php — GET/POST /coordinator/ticker/{id}

declare(strict_types=1);

require_coordinator();

$ticker_id = (int)($_REQUEST['ticker_id'] ?? 0);
$pdo = get_db();

// Ownership check: ticker must belong to coordinator's team
$stmt = $pdo->prepare(
    "SELECT id, name, description, status FROM tickers WHERE id = ? AND team_id = ?"
);
$stmt->execute([$ticker_id, $_SESSION['team_id']]);
$ticker = $stmt->fetch(PDO::FETCH_ASSOC);

if (!$ticker) {
    http_response_code(404);
    echo '<h1>Ticker nicht gefunden</h1>';
    exit;
}

$error       = '';
$edit_message = null; // When ?edit_message_id is set, pre-fill edit form

// ── Handle POST actions ──────────────────────────────────────────────────────
if ($_SERVER['REQUEST_METHOD'] === 'POST') {
    require_csrf();
    $action = trim($_POST['action'] ?? '');

    if ($action === 'post_message') {
        $message   = trim($_POST['message'] ?? '');
        $timestamp = trim($_POST['timestamp'] ?? date('H:i'));
        $tag_id    = !empty($_POST['tag_id']) ? (int)$_POST['tag_id'] : null;

        if (mb_strlen($message, 'UTF-8') === 0) {
            $error = 'Nachricht darf nicht leer sein.';
        } elseif (mb_strlen($message, 'UTF-8') > 280) {
            $error = 'Nachricht darf maximal 280 Zeichen lang sein.';
        } elseif (!preg_match('/^\d{2}:\d{2}$/', $timestamp)) {
            $error = 'Uhrzeit muss Format HH:MM haben.';
        } else {
            $stmt = $pdo->prepare(
                "INSERT INTO ticker_messages (ticker_id, tag_id, message, timestamp, created_at, updated_at)
                 VALUES (?, ?, ?, ?, NOW(), NOW())"
            );
            $stmt->execute([$ticker_id, $tag_id, $message, $timestamp]);
            redirect("/coordinator/ticker/$ticker_id");
        }
    }

    if ($action === 'delete_message') {
        $msg_id = (int)($_POST['message_id'] ?? 0);
        if ($msg_id > 0) {
            // Verify message belongs to this ticker (ownership via ticker → team)
            $stmt = $pdo->prepare(
                "DELETE FROM ticker_messages WHERE id = ? AND ticker_id = ?"
            );
            $stmt->execute([$msg_id, $ticker_id]);
        }
        redirect("/coordinator/ticker/$ticker_id");
    }

    if ($action === 'edit_message') {
        $msg_id    = (int)($_POST['message_id'] ?? 0);
        $message   = trim($_POST['message'] ?? '');
        $timestamp = trim($_POST['timestamp'] ?? date('H:i'));
        $tag_id    = !empty($_POST['tag_id']) ? (int)$_POST['tag_id'] : null;

        if (mb_strlen($message, 'UTF-8') === 0) {
            $error = 'Nachricht darf nicht leer sein.';
        } elseif (mb_strlen($message, 'UTF-8') > 280) {
            $error = 'Nachricht darf maximal 280 Zeichen lang sein.';
        } elseif (!preg_match('/^\d{2}:\d{2}$/', $timestamp)) {
            $error = 'Uhrzeit muss Format HH:MM haben.';
        } elseif ($msg_id > 0) {
            $stmt = $pdo->prepare(
                "UPDATE ticker_messages
                 SET message = ?, timestamp = ?, tag_id = ?, updated_at = NOW()
                 WHERE id = ? AND ticker_id = ?"
            );
            $stmt->execute([$message, $timestamp, $tag_id, $msg_id, $ticker_id]);
            redirect("/coordinator/ticker/$ticker_id");
        }
    }
}

// ── GET: fetch messages + tags + freigabe members ────────────────────────────

// If ?edit_message_id=X, pre-load that message for the edit form
$edit_msg_id = (int)($_GET['edit_message_id'] ?? 0);
if ($edit_msg_id > 0) {
    $stmt = $pdo->prepare("SELECT * FROM ticker_messages WHERE id = ? AND ticker_id = ?");
    $stmt->execute([$edit_msg_id, $ticker_id]);
    $edit_message = $stmt->fetch(PDO::FETCH_ASSOC) ?: null;
}

$stmt = $pdo->prepare(
    "SELECT m.id, m.message, m.timestamp, m.tag_id, m.created_at,
            t.label AS tag_label, t.color AS tag_color
     FROM ticker_messages m
     LEFT JOIN ticker_tags t ON m.tag_id = t.id
     WHERE m.ticker_id = ?
     ORDER BY m.created_at DESC"
);
$stmt->execute([$ticker_id]);
$messages = $stmt->fetchAll(PDO::FETCH_ASSOC);

$stmt = $pdo->prepare(
    "SELECT id, label, color FROM ticker_tags WHERE team_id = ? ORDER BY sort_order, created_at"
);
$stmt->execute([$_SESSION['team_id']]);
$tags = $stmt->fetchAll(PDO::FETCH_ASSOC);

// Freigabe members for info display
$stmt = $pdo->prepare(
    "SELECT u.first_name, u.last_name
     FROM ticker_members tm
     JOIN users u ON tm.user_id = u.id
     WHERE tm.ticker_id = ? AND tm.team_id = ?"
);
$stmt->execute([$ticker_id, $_SESSION['team_id']]);
$freigabe_members = $stmt->fetchAll(PDO::FETCH_ASSOC);

require ROOT_PATH . '/src/templates/coordinator/layout.php';

render_coach_page(e($ticker['name']), 'ticker', function() use ($ticker, $messages, $tags, $freigabe_members, $error, $edit_message, $ticker_id) {
    if ($error) echo '<div class="alert alert-danger">' . e($error) . '</div>';
    require ROOT_PATH . '/src/templates/coordinator/ticker_detail.php';
});
```

### 2. src/coordinator/ticker_close_handler.php
POST /coordinator/ticker/{id}/close — set status to closed.

```php
<?php
// src/coordinator/ticker_close_handler.php — POST /coordinator/ticker/{id}/close

declare(strict_types=1);

require_coordinator();
require_csrf();

$ticker_id = (int)($_REQUEST['ticker_id'] ?? 0);
$pdo = get_db();

$stmt = $pdo->prepare(
    "UPDATE tickers SET status = 'closed', updated_at = NOW()
     WHERE id = ? AND team_id = ?"
);
$stmt->execute([$ticker_id, $_SESSION['team_id']]);

redirect('/coordinator/ticker?success=closed');
```

### 3. src/coordinator/ticker_delete_handler.php
GET: show confirmation page. POST: delete ticker (messages cascade via FK).

```php
<?php
// src/coordinator/ticker_delete_handler.php — GET/POST /coordinator/ticker/{id}/delete

declare(strict_types=1);

require_coordinator();

$ticker_id = (int)($_REQUEST['ticker_id'] ?? 0);
$pdo = get_db();

$stmt = $pdo->prepare(
    "SELECT id, name FROM tickers WHERE id = ? AND team_id = ?"
);
$stmt->execute([$ticker_id, $_SESSION['team_id']]);
$ticker = $stmt->fetch(PDO::FETCH_ASSOC);

if (!$ticker) {
    http_response_code(404);
    echo '<h1>Ticker nicht gefunden</h1>';
    exit;
}

if ($_SERVER['REQUEST_METHOD'] === 'POST') {
    require_csrf();

    $stmt = $pdo->prepare("DELETE FROM tickers WHERE id = ? AND team_id = ?");
    $stmt->execute([$ticker_id, $_SESSION['team_id']]);

    redirect('/coordinator/ticker?success=deleted');
}

// GET: show confirmation page
require ROOT_PATH . '/src/templates/coordinator/layout.php';

render_coach_page('Ticker löschen', 'ticker', function() use ($ticker) {
    require ROOT_PATH . '/src/templates/coordinator/ticker_delete_confirm.php';
});
```

### 4. src/templates/coordinator/ticker_detail.php
Message feed (newest first) + post form + edit form (conditional) + message edit/delete buttons.

```php
<?php
// src/templates/coordinator/ticker_detail.php
// Variables: $ticker, $messages, $tags, $freigabe_members, $error, $edit_message, $ticker_id
?>
<div class="d-flex justify-content-between align-items-start mb-3">
    <div>
        <h4 class="fw-semibold mb-1"><?= e($ticker['name']) ?></h4>
        <?php if ($ticker['description']): ?>
        <p class="text-muted small mb-0"><?= e($ticker['description']) ?></p>
        <?php endif; ?>
    </div>
    <div class="d-flex gap-2 ms-3 flex-shrink-0">
        <?php if ($ticker['status'] === 'active'): ?>
        <span class="badge bg-success align-self-center">Aktiv</span>
        <form method="POST" action="/coordinator/ticker/<?= (int)$ticker['id'] ?>/close" class="d-inline">
            <?= csrf_field() ?>
            <button type="submit" class="btn btn-sm btn-outline-warning">Schließen</button>
        </form>
        <?php else: ?>
        <span class="badge bg-secondary align-self-center">Geschlossen</span>
        <?php endif; ?>
        <a href="/coordinator/ticker/<?= (int)$ticker['id'] ?>/delete" class="btn btn-sm btn-outline-danger">Löschen</a>
    </div>
</div>

<?php if (!empty($freigabe_members)): ?>
<p class="text-muted small mb-3">
    <i class="bi bi-people me-1"></i>Freigegebene Mitglieder:
    <?= e(implode(', ', array_map(fn($m) => $m['first_name'] . ' ' . $m['last_name'], $freigabe_members))) ?>
</p>
<?php endif; ?>

<?php if ($ticker['status'] === 'closed'): ?>
<div class="alert alert-info" role="alert">Dieser Ticker ist geschlossen.</div>
<?php endif; ?>

<!-- Post form (coordinator can always post, even on closed ticker for corrections) -->
<?php if ($edit_message): ?>
<!-- Edit message form -->
<div class="card shadow-sm mb-4">
    <div class="card-header fw-semibold">Nachricht bearbeiten</div>
    <div class="card-body">
        <form method="POST" action="/coordinator/ticker/<?= (int)$ticker_id ?>">
            <?= csrf_field() ?>
            <input type="hidden" name="action" value="edit_message">
            <input type="hidden" name="message_id" value="<?= (int)$edit_message['id'] ?>">
            <div class="mb-3">
                <label class="form-label">Nachricht (max. 280 Zeichen)</label>
                <textarea name="message" class="form-control" rows="3" maxlength="280" required
                          oninput="updateCounter(this, 'editCharCount')"><?= e($edit_message['message']) ?></textarea>
                <small class="form-text text-muted"><span id="editCharCount"><?= mb_strlen($edit_message['message'], 'UTF-8') ?></span>/280 Zeichen</small>
            </div>
            <div class="row">
                <div class="col-6 mb-3">
                    <label class="form-label">Uhrzeit (HH:MM)</label>
                    <input type="time" name="timestamp" class="form-control" value="<?= e($edit_message['timestamp']) ?>" required>
                </div>
                <div class="col-6 mb-3">
                    <label class="form-label">Tag/Kategorie</label>
                    <select name="tag_id" class="form-select">
                        <option value="">Kein Tag</option>
                        <?php foreach ($tags as $tag): ?>
                        <option value="<?= (int)$tag['id'] ?>" <?= (int)$edit_message['tag_id'] === (int)$tag['id'] ? 'selected' : '' ?>>
                            <?= e($tag['label']) ?>
                        </option>
                        <?php endforeach; ?>
                    </select>
                </div>
            </div>
            <div class="d-flex gap-2">
                <button type="submit" class="btn btn-primary">Speichern</button>
                <a href="/coordinator/ticker/<?= (int)$ticker_id ?>" class="btn btn-outline-secondary">Abbrechen</a>
            </div>
        </form>
    </div>
</div>
<?php else: ?>
<!-- New message post form -->
<div class="card shadow-sm mb-4">
    <div class="card-header fw-semibold">Nachricht posten</div>
    <div class="card-body">
        <form method="POST" action="/coordinator/ticker/<?= (int)$ticker_id ?>">
            <?= csrf_field() ?>
            <input type="hidden" name="action" value="post_message">
            <div class="mb-3">
                <label class="form-label">Nachricht (max. 280 Zeichen)</label>
                <textarea name="message" id="message" class="form-control" rows="3"
                          maxlength="280" required placeholder="Gib eine Nachricht ein…"
                          oninput="updateCounter(this, 'charCount')"></textarea>
                <small class="form-text text-muted"><span id="charCount">0</span>/280 Zeichen</small>
            </div>
            <div class="row">
                <div class="col-6 mb-3">
                    <label class="form-label">Uhrzeit (HH:MM)</label>
                    <input type="time" name="timestamp" class="form-control" value="<?= date('H:i') ?>" required>
                </div>
                <div class="col-6 mb-3">
                    <label class="form-label">Tag/Kategorie</label>
                    <select name="tag_id" class="form-select">
                        <option value="">Kein Tag</option>
                        <?php foreach ($tags as $tag): ?>
                        <option value="<?= (int)$tag['id'] ?>"><?= e($tag['label']) ?></option>
                        <?php endforeach; ?>
                    </select>
                </div>
            </div>
            <button type="submit" class="btn btn-primary">Posten</button>
        </form>
    </div>
</div>
<?php endif; ?>

<script>
function updateCounter(textarea, counterId) {
    document.getElementById(counterId).textContent = textarea.value.length;
}
document.addEventListener('DOMContentLoaded', function() {
    const ta = document.getElementById('message');
    if (ta) updateCounter(ta, 'charCount');
});
</script>

<!-- Message feed (newest first, D-05) -->
<h4 class="fw-semibold mb-3">Nachrichten (<?= count($messages) ?>)</h4>

<?php if (empty($messages)): ?>
<p class="text-muted text-center py-4">Noch keine Nachrichten. Stelle eine Nachricht bereit, um den Ticker zu füllen.</p>
<?php else: ?>
<?php foreach ($messages as $msg): ?>
<div class="card mb-2">
    <div class="card-body py-2 px-3">
        <div class="d-flex justify-content-between align-items-start">
            <div>
                <strong><?= e($msg['timestamp']) ?></strong>
                <?php if ($msg['tag_id']): ?>
                <span class="badge bg-<?= e($msg['tag_color'] ?? 'secondary') ?> ms-1">
                    <?= e($msg['tag_label'] ?? '') ?>
                </span>
                <?php endif; ?>
                <p class="mb-0 mt-1"><?= e($msg['message']) ?></p>
            </div>
            <div class="d-flex gap-1 ms-3 flex-shrink-0">
                <a href="/coordinator/ticker/<?= (int)$ticker_id ?>?edit_message_id=<?= (int)$msg['id'] ?>"
                   class="btn btn-outline-secondary btn-sm">Bearbeiten</a>
                <form method="POST" action="/coordinator/ticker/<?= (int)$ticker_id ?>" class="d-inline">
                    <?= csrf_field() ?>
                    <input type="hidden" name="action" value="delete_message">
                    <input type="hidden" name="message_id" value="<?= (int)$msg['id'] ?>">
                    <button type="submit" class="btn btn-outline-danger btn-sm"
                            onclick="return confirm('Nachricht löschen?')">Löschen</button>
                </form>
            </div>
        </div>
    </div>
</div>
<?php endforeach; ?>
<?php endif; ?>
```

### 5. src/templates/coordinator/ticker_delete_confirm.php
Two-step delete confirmation (no JS required — uses form submit).

```php
<?php
// src/templates/coordinator/ticker_delete_confirm.php
// Variables: $ticker (array with id, name)
?>
<div class="card border-danger shadow-sm" style="max-width: 500px;">
    <div class="card-header bg-danger text-white fw-semibold">
        <i class="bi bi-exclamation-triangle me-2"></i>Ticker löschen
    </div>
    <div class="card-body">
        <p class="mb-2">Du bist dabei, den Ticker <strong><?= e($ticker['name']) ?></strong> zu löschen.</p>
        <p class="text-danger mb-4">Dieser Ticker und alle Nachrichten werden dauerhaft gelöscht. Diese Aktion kann nicht rückgängig gemacht werden.</p>
        <form method="POST" action="/coordinator/ticker/<?= (int)$ticker['id'] ?>/delete">
            <?= csrf_field() ?>
            <div class="d-flex gap-2">
                <button type="submit" class="btn btn-danger">Jetzt löschen</button>
                <a href="/coordinator/ticker/<?= (int)$ticker['id'] ?>" class="btn btn-outline-secondary">Abbrechen</a>
            </div>
        </form>
    </div>
</div>
```
  </action>
  <verify>
    <automated>php -l /Users/sebastianwiller/Documents/github/team-manager/src/coordinator/ticker_detail_handler.php && php -l /Users/sebastianwiller/Documents/github/team-manager/src/coordinator/ticker_close_handler.php && php -l /Users/sebastianwiller/Documents/github/team-manager/src/coordinator/ticker_delete_handler.php && php -l /Users/sebastianwiller/Documents/github/team-manager/src/templates/coordinator/ticker_detail.php && php -l /Users/sebastianwiller/Documents/github/team-manager/src/templates/coordinator/ticker_delete_confirm.php</automated>
  </verify>
  <done>All 5 files exist and pass php -l. ticker_detail_handler.php handles post_message/edit_message/delete_message actions with mb_strlen validation. ticker_close_handler.php updates status to closed. ticker_delete_handler.php does GET (confirm page) + POST (delete). ticker_delete_confirm.php has Gefahrenzone card with danger styling and POST form.</done>
</task>

</tasks>

<verification>
- All 9 files pass `php -l`
- `grep "require_coordinator" src/coordinator/ticker_*.php` returns a match in each handler
- `grep "mb_strlen" src/coordinator/ticker_detail_handler.php` returns a match
- `grep "ticker_members" src/coordinator/ticker_create_handler.php` returns a match
- `grep "dauerhaft gelöscht" src/templates/coordinator/ticker_delete_confirm.php` returns a match
- `grep "edit_message_id" src/templates/coordinator/ticker_detail.php` returns a match
</verification>

<success_criteria>
5 coordinator ticker handler files + 4 coordinator ticker templates created. All pass php -l. Handlers follow PRG pattern, all POST forms include CSRF tokens, message validation uses mb_strlen. Ticker delete is two-step (GET confirm, POST delete). Messages cascade on ticker delete via FK ON DELETE CASCADE.
</success_criteria>

<output>
After completion, create `.planning/phases/07-live-ticker/07-03-SUMMARY.md`
</output>
